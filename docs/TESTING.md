# Testing

Normal validation is offline and must not publish or call production providers.

## Verified Phase 0D evidence

The Phase 0D exact reviewed head and merge CI passed Node 22 offline quality gates, PostgreSQL 16 and 18 integration, AgentShield 1.4.0, and workflow/YAML validation. The executable offline suites reported posting 52, image 10, orchestrator 81, gate 56, and API 51 passing checks; the Render identity/startup suite reports a single invariant-suite PASS. Each disposable PostgreSQL version reported 86 passing integration assertions (fresh 12, upgrade 33, durable 41), and each bound HTTP suite reported 54 passing assertions. These numbers are evidence from the current executable suites, not a promised permanent count.

After the Phase 0D merge, the production workflow's authorization job failed closed because `RENDER_DEPLOY_AUTOMATION_ENABLED=false`, and its deploy job was skipped. That is expected disabled-gate evidence, not a failed application release and not proof that the controller has operated in production. The current Phase 0D commit was separately observed live on all three Render services through the previous native auto-deploy path; API health and worker readiness matched the exact commit. A normal scheduled execution of that same commit was subsequently observed on 2026-08-25, closing that gap; the run evidence is recorded in [Status](STATUS.md). Do not trigger production cron to produce such evidence.

## Phase 0B.0 local evidence — 2026-08-27, re-verified at the merge commit 2026-08-28, deployed 2026-08-28

Recorded from actual runs, not promised as permanent counts. Re-run on 2026-08-28 against the merge commit `44d7336…`: `npm run test:offline` reported **362 passing checks across eight suites**; disposable PostgreSQL **16** reported **154 passing integration assertions** (fresh 32, upgrade 53, durable 69), with `006_content_evidence.sql` applied in both the fresh and upgrade paths; the bound HTTP suite reported **68 passing assertions** — 64 from the branch plus the four added by the `/console/*` body-drain security fix, whose regression test opens a raw keep-alive socket, pipelines a second request behind a rejected partial preview, and proves no desynchronized second response is ever produced. AgentShield 1.4.0 exited 0 with grade A and zero findings. Typecheck, build, simulated dry run, deployment-controller fixtures, Markdown links, environment coverage (35 variables), sensitive-content scan, `npm audit --omit=dev` (0 vulnerabilities), and `git diff --check` were all clean.

**Migration 006 was also measured directly, not only exercised.** Applied inside a transaction against a `001–005` baseline, a `pg_locks` join against `approval_queue`, `approval_decisions`, `media`, `brief_queue`, `events`, and `session_state` returned zero rows — it takes no lock on any pre-existing table. Standalone wall time was **49 ms**; the full `001–006` fresh run reported 156 ms. Separately, `a6a4316…` was checked out, built, and run against a runner-migrated `001–006` database: its durable startup probe passed and it read the queue correctly, which is the empirical basis for the rollback posture in [ROLLOUT_PHASE_0B0.md](ROLLOUT_PHASE_0B0.md).

One caveat found while measuring: migration 006's `SET LOCAL lock_timeout`/`statement_timeout` guards are only active inside a transaction. Applied by hand through `psql -f` without an explicit `BEGIN`, PostgreSQL warns and both guards are silently inactive. It must be applied by `npm run migrate`, which wraps each file in `BEGIN … COMMIT`.

**Production deployment, 2026-08-28 — independently verified** by a separate final-inspection session with read-only PostgreSQL access: migration 006 is applied to production exactly once, and the catalog inventory on the deployed database matched the corrected count in [ROLLOUT_PHASE_0B0.md §2](ROLLOUT_PHASE_0B0.md) exactly — 2 tables, 10 indexes, 16 CHECK constraints, 3 foreign keys, 2 primary keys, 1 trigger. The ~53 ms application duration, matching this local evidence, is operator-reported, not independently re-derived. `S5. preview is deterministic for a fixed trace and clock` (`src/harness/contentIntelligence.selftest.ts`) is the automated, in-process determinism proof for the preview route, run in this same CI matrix on every PR. It is what established deterministic equality for the rollout: the operator authorization granted exactly one production preview call, exactly one was made, and **no second production call occurred** — a documented variance from step 13's written two-call procedure, recorded in [ROLLOUT_PHASE_0B0.md §0](ROLLOUT_PHASE_0B0.md).

**PostgreSQL 18 was not run locally for this change.** The engineering session's egress policy blocks both the Docker registry blob host (`production.cloudfront.docker.com`, 403) and `apt.postgresql.org` (403), and only a PostgreSQL 16 cluster is installed. That gap is closed by CI, whose two-version matrix runs the same suite on 16 and 18. On the Phase 0B.0 exact head all five jobs passed, and the PostgreSQL 18 job was verified from its logs rather than from the summary API: the service container is `postgres18`, and every migration-006 rejection constraint is visible firing in that server's log — `verified_requires_source`, `kind_check`, `claim_present`, `confidence_range`, `performance_shape`, `observation_requires_observed_at`, `causal_not_certain`, `assumption_low_confidence`, `supersession_shape`, `no_self_supersede`, and `relations_no_self`. Class separation is enforced by PostgreSQL 18, not merely asserted.

```bash
npm ci
npm run build
npm run typecheck
npm run test:posting
npm run test:image
npm run test:orchestrator
npm run test:gate
npm run test:api
npm run test:render-identity
npm run test:ownership
npm run test:content-intelligence
npm run dryrun
npm run test:deployment-controller
npm run check:markdown-links
npm run check:env-coverage
npm run scan:sensitive
npm audit --omit=dev
git diff --check
```

The self-tests are executable scripts, not a unified test framework:

- `test:posting` validates pure native request builders and strict package/target/full-subject validation, including rejection of empty arrays, malformed siblings, and duplicate platforms. Adversarial exact-payload matcher/approval checks cover fabricated booleans/IDs, an ephemeral approval at the durable publication boundary, wrong index, changed copy/media/CTA/destination, unknown provider-ignored fields, runtime destination mismatch, expiry, and revocation. Module-private mocked-transport probes cover an issued guard's mid-flow revocation recheck, guard execution before every provider attempt (including Instagram status GETs/retries), rejection of a caller-forged native guard before fetch, redirect refusal, and 2xx responses missing provider post IDs. It does not inject a fake provider into the sanctioned publication entrypoint, tamper a stored database subject, or exercise a live durable hosted-media row.
- `test:image` validates fal model routing/request shape and a fake provider. It proves the request carries the provider-friendly **source** size rather than the publication size (1080x1350 requests 1024x1280), that every reviewed source mapping has exactly its target aspect ratio, that an unmapped profile falls back to the target itself, and that provider metadata is preserved when present while absent `width`/`height` never fail generation.
- `test:orchestrator` additionally covers publication-media normalization against the August 2026 incident: a raw 896x1120 PNG passes decode safety but is refused as final publication media; 896x1120 and 1024x1280 both normalize to exactly 1080x1350 JPEG by a pure uniform scale; a 1024x1024 render is **refused rather than cropped** and is typed as a deterministic, non-retryable media-contract failure that spends exactly one generation; malformed and oversized inputs are rejected; QC, hosting, and the content digest all operate on the same normalized bytes; and the durable publication guard still rejects any non-profile size, with the four approved profiles unchanged.
- `test:orchestrator` validates JSON parsing, formatter → canonical validation → critic ordering, pass/revision/escalation cycles, canonical-fact override resistance while preserving scheduler context, approval-bound destination injection/fail-closed missing target, production rejection of injected runner/resolver/target and vision-inspector seams, trusted fal host/credential/port policy, malformed/garbled/unsafe QC and a Facebook-only critic-requested image revision, normalization to the four feed profiles plus pure input-header/decode checks, Slack-summary mention/link/markup neutralization, explicit language handling, provider-visible hashtag/media/destination constraints, recursive freeze, and that orchestration never invokes posting. It directly exercises the shared publication-media policy with an allowed-profile JPEG plus non-JPEG and over-5-MiB rejection; static source wires that same pure helper into the live guard. Its injected image resolver does not exercise live download; the Jimp quality-90 transcode and output-header assertions are now exercised directly by the normalization coverage above, against real generated image bytes.
- `test:gate` validates the worker delivery requirement for a strict Slack webhook in every environment, production review-origin rules, non-redirecting bounded Slack request shape, an inert code-delimited approval summary whose generated review URL is the only active link, and adversarial escalation goal/reason/run-ID neutralization; exact Instagram host validation; in-memory queue/media-copy compatibility; durable-state/publication refusal; canonical JSON/hash stability; hash-only expiring decision tokens; atomic terminal decisions; authorization expiry/revocation; exact ephemeral subject resolution; and the absence of an autonomy/boolean bypass.
- `test:api` validates shared-secret fail-closed behavior, accepted header forms/query rejection, trigger schema/content-type/body/time bounds, generic limiter bounds, diagnostic operation timeout, strict content-addressed and legacy media-path parsing, and review visibility/validation of hash, destination, digest, token exclusion, and provider fields without starting the API or calling providers.
- `test:ownership` validates exclusive worker ownership and interrupted-brief recovery offline with injected boundaries: advisory-key stability against its documented namespace, uncontended acquisition, contention in which a second instance acquires only after the holder's session ends, non-blocking `pg_try_advisory_lock` polling rather than one blocking wait, connection-error and heartbeat loss both fencing side effects, the absence of any reconnect/reacquire path, and aborted acquisition closing its dedicated session. It classifies every interruption phase from durable markers, proves partial known publication preserves provider post IDs and is never reported as zero mutation, and proves a fully settled run is reconstructed rather than lost. It proves the audit event is written before the status change, that recovery never writes `pending` and never calls a provider, and that the orphan sweep spares approvals owned by a real brief. For publication it proves the ordering barrier → started marker → provider → settled marker, that a failed started marker makes no provider call at all, that a failed settled marker after provider success becomes a distinct uncertain outcome that stops all remaining platforms, and that ownership lost before or between platforms prevents any further provider contact. Static assertions confirm the worker actually wires these boundaries and that the recovery module imports no provider code.
- `test:ownership` additionally **executes** the worker fencing rules rather than asserting on source text: ownership lost while orchestration is still computing (no approval is ever created), lost before approval creation, lost before credential acquisition (no token call, no provider call), lost after the started marker commits but before the provider boundary (the second barrier blocks the request), and lost between platforms (the next platform is never contacted). It proves a former owner declines every terminal write so it cannot overwrite a successor's recovery, that the owner path still writes both terminal records, that a hung ownership statement is bounded client-side and destroys its socket, that a former owner cannot claim, and that the ownership-loss exit path is nonzero, never releases the lock, and stops the recurring timers that would otherwise keep the process alive. Remaining static checks are import-boundary facts only.
- `test:render-identity` validates local identity behavior plus exact production full-commit requirements, optional safe Render instance correlation, PostgreSQL-only worker readiness and API health construction, and worker startup ordering. Failed, throwing, or still-pending state/provider initialization and identity failure cannot emit readiness or enter queue consumption.
- `test:content-intelligence` validates the Phase 0B.0 evidence contract, evidence pack, approved-facts adapter, agent registry, and the Content Intelligence preview, **the Phase 0B.1 `strategy-concept`, Phase 0B.2 `automotive-truth`, Phase 0B.3 `hook-story-script`, Phase 0B.4 `production-direction`, Phase 0B.5 `packaging-adaptation`, and Phase 0B.6 `final-critic` executors** offline, with no database and no provider. 969 checks (1,367 across all eight offline suites, directly counted: posting 52, image 18, orchestrator 108, gate 56, API 51, render-identity 1 invariant-suite pass, ownership 112, content-intelligence 969). Every model call in the suite goes through an **injected fake runner**; no test contacts Anthropic or any network, and CI makes no external model request. **`final-critic` (Phase 0B.6) is merged to `main` through PR #52**, on the same terms as the five stages before it: dormant, `executionEnabled: false`, and reached by no production path.
- PR #50's exact reviewed head `d901896dda7484b062a0975916ad7a977c4fb904` passed all five GitHub jobs before merge: Node 22 offline quality gates, PostgreSQL 16 integration, PostgreSQL 18 integration, AgentShield 1.4.0, and workflow/YAML static validation. AgentShield reported grade A, score 94, with no critical or high findings; the production dependency audit reported zero vulnerabilities. These are repository-validation facts for the dormant executor, not deployment or production evidence.
- The offline suite is verified to pass with a **nonempty `ANTHROPIC_API_KEY`** exported in the parent process. This is a real property, not an assumption: `config.anthropicApiKey` is captured once at module initialization and `getClient()` reads the captured value, so clearing the environment variable inside a test would not prevent a live call. The single test that exercises the production runner clears `config.anthropicApiKey` in `try/finally`, restores the exact prior value, and asserts the failure is the missing-credential error rather than any network outcome. None of the six stage executors has a default runner to fall back to — a caller must supply one.
- What the executor tests prove is narrow and is stated as such in the suite: **wrong-class and fabricated ids cannot enter the typed channels**, and **no sentence the model writes becomes a citation or a permitted claim**. They do **not** prove that model prose is true. A test deliberately feeds a drifting restatement through validation, confirms it passes, and then confirms that its wording appears in neither the returned evidence records nor the permitted-claim text — those accessors return the exact records bound by the cited ids.

  The one place the *production* runner is touched is its fail-closed credential path, and that assertion controls the value the code actually reads. `config.anthropicApiKey` is captured from the environment once at module initialization and `getClient()` reads the captured value, so clearing `process.env.ANTHROPIC_API_KEY` after import would change nothing — on a machine whose parent process exports a real key, an earlier version of this test would have made a live API request. The test now clears `config.anthropicApiKey` inside `try/finally`, restores the exact prior value, and asserts the failure message is the missing-credential error rather than any network outcome. It is verified to pass with a nonempty key exported in the parent process.

  Executor coverage proves: a valid invocation validates; the exact checked-in prompt and skill assets reach the instruction channel while **reference contents reach neither `systemPrompt` nor the payload**, with asset metadata recording the channel each asset actually reached; allowed factual claims reach the model only through the shared classified evidence projection, whose actual payload includes authoritative `kind` but no confidence, provenance, reviewer identity, or internal timestamp; and reference data cannot widen `allowedFacts`. Stage 2 requires the complete typed Stage 1 output, places every field in one bounded `STRATEGY_OUTPUT` untrusted-data block, exposes misleading angle and rationale prose for review, and permits none of that input through `allowedClaimRecords()` or `allowedClaimTexts()`. Empty, malformed, and oversized Stage 1 inputs fail before the runner. Required fact classes are enforced **before** any model call; fabricated, stale, conflicted, inactive, and wrong-class citations fail; hypotheses, performance evidence, sourced research, and unsupported assumptions cannot enter the typed fact-citation channel in either direction; malformed JSON, prose-wrapped and fenced output, missing and extra fields, empty and oversized values, wrong types, and unknown enum values fail closed; missing assets, a runner error, a timeout, empty model text, and missing credentials fail closed; one invocation causes at most one request with no retry construct in source; only the declared capability is available; explicit scheduler, orchestrator, approval, publication, image/Slack, API/preview, worker, and database/evidence-write boundary checks prove dormancy; and all six stages remain `executionEnabled: false`.

  **The prose boundary is tested for what it actually is.** A Phase 0B.1 regression feeds misleading prose — a performance→automotive promotion asserted as settled fact, alongside a real but unrelated valid fact id — and asserts that it *validates* (the validator does not read meaning and does not claim to) while never becoming evidence: the prose is branded `provisional_model_prose` with `verified: false` and `publishable: false`, `citedFactRecords()` returns only the cited pack record, no returned record carries the prose, and the module exports no prose-to-evidence conversion. A Phase 0B.2 regression passes the complete result onward and proves its misleading angle and rationale are visible but cannot enter the permitted-claim accessors. Stage 2 structurally whitelists exact evidence-record ids; it does not semantically prove prose or restatements true. A further check asserts the injected `skills/brand-voice/SKILL.md` claims no make outside `config/approved-facts.json` and asserts no location count. **Phase 0B.3 tests the authority boundary and its limit together.** They prove that a real, citable pack fact stage 2 did not permit is absent from the projection and fails if cited; that the complete pack is never rendered as an alternate claim source; that both prior-stage outputs are revalidated against the pack, with malformed, incomplete, wrongly branded, evidence-inconsistent (a whitelist naming an id this pack does not permit), and oversized handoffs all refused with **zero** model requests, while a JSON-round-tripped valid pair of prior-stage outputs executes normally with exactly one runner call — the check is structural, not a provenance or authenticity check; and that an empty whitelist refuses before the model call. They then demonstrate the limit honestly: a drifting paraphrase and several uncited factual assertions **validate**. The drifting paraphrase and uncited script wording do not appear in either accessor result; `scriptClaimRecords()` and `scriptClaimTexts()` still return the exact permitted evidence record bound by the cited id. Deterministic validation checks structure, bounds, enums, ids, and whitelist membership; it does not prove a paraphrase faithful or detect every uncited implication, and no test claims it does. A separate group asserts the injected `skills/script-craft/SKILL.md` contains no approved-fact value, vehicle make, address, warranty figure, slogan, service capability, or CTA destination, and that `agents/hook-story-script.md` declares `tools: []` and pins no model. **Phase 0B.4 tests the narrowest authority boundary and its visual limits together.** They prove that only the records stage 3 actually used appear in `SCRIPT_CLAIMS`; that a stage 2-permitted but stage 3-unused fact and a pack fact outside stage 2's whitelist are both absent and both fail if cited; that the complete pack and stage 2's provisional prose never reach the model payload; that malformed, wrongly branded, evidence-inconsistent and incomplete prior-stage values fail with **zero** model requests while JSON-round-tripped valid ones execute with exactly one — structural validation, not a provenance check; and that an empty stage 3 claim-use set refuses before the model call. They then demonstrate the limits honestly: a drifting overlay, a shot depicting an unestablished before-and-after, an uncited on-camera endorsement, and a requirement asserting an asset is available all **validate**, stay branded unverified and non-executable, and appear in no accessor result — while the accessors still return the exact record bound by the cited id. A separate group asserts `skills/production-craft/SKILL.md` contains no approved-fact value, make, address, locality, service fact, warranty or automotive figure, slogan, CTA destination, provider or model name, platform profile, or runtime generation/QC instruction, and that `agents/production-direction.md` declares `tools: []` and pins no model. **Phase 0B.5 tests the platform boundary and its semantic limits together.** They prove that only stage 3-used records reach `SCRIPT_CLAIMS`; that a stage 2-permitted-but-unused fact, a pack-only fact, stage 2's prose and the complete pack never reach the model; that stage 4's direction, overlay and requirement prose is present as context but is never a claim source; that all four rejected placeholder assets are absent from the instruction channel and preserved for their existing consumers; that malformed, incomplete, wrongly branded, evidence-inconsistent and oversized prior-stage values, and empty, duplicate, unknown or oversized platform lists, all fail with **zero** model requests while JSON-round-tripped valid values execute with exactly one; and that an empty stage 3 used-claim set refuses independently before the model call. Per-platform policy is enforced deterministically against the constants and single hashtag tokenizer `packageMap.ts` already applies to provider text — Instagram 8–15 hashtags and 2,200 provider-visible characters, Facebook at most 2, GBP zero and 1,500 — with caption hashtags, invalid array tokens, case-insensitive duplicates, and combined caption/separator/tag overflow refused. Exhaustive forward and inverse maps plus both runtime round trips bind `google_business_profile` to provider `gbp` without collapsing the vocabularies. Dedicated URL-bearing fields are structurally absent, while one recognizable-URL guard rejects explicit schemes and `www.` tokens in caption, local keywords, open questions, and claim-use summaries; tests do not claim that obfuscated or semantic destination prose is detectable. Exactly one package per requested platform remains required in the requested order. They then demonstrate the semantic limits honestly: a drifting caption, an unsupported local keyword and a useless recommended time all **validate**, stay branded unverified, non-publishable, non-executable and non-schedulable, and appear in no accessor result — while the accessors still return the exact record bound by the cited id. **Eight deliberate executor mutations confirm those assertions are load-bearing**: the original five plus removal of the caption hashtag guard, combined provider-text limit, and four-channel URL guard. The executor was restored byte-for-byte after each. The earlier exercise found a real weakness: the claim-binding regressions originally used a catch-any-throw helper and would have passed on an incidental `TypeError`, so the whole section now requires the stage's own typed `StageExecutionError`. **Phase 0B.6 tests the last authority boundary — one stage narrower than 0B.5's — together with the honest guarantee that this stage never approves anything.** They prove that `PLATFORM_CLAIMS` holds only stage 5's own typed, per-platform claim bindings — narrower than `SCRIPT_CLAIMS` — read only through stage 5's evidence accessor; that the complete pack, stage 2's prose and whitelist, and `config/approved-facts.json` never reach the model; that all four prior-stage values (stages 2–5) are revalidated with **zero** model requests on failure while a JSON-round-tripped valid chain executes with exactly one; and that a requested-platform list mismatching stage 5's own package sequence — by membership or by order alone — fails closed. They then prove the honest-limit guarantee directly: a model output that tries to smuggle an approval field onto the assessment is refused, not silently dropped, because the output contract has no field through which to set one, and even a maximally confident, all-clear verdict still carries all five literal-`false` brands (`authoritative`, `approvalGranted`, `publishable`, `executable`, `productionValidated`). Structural verdict-consistency checks are anchored on each finding's `severity` **and** `owner` (`hook-story-script` | `production-direction` | `packaging-adaptation` | `human_review`) — `provisional_pass` cannot coexist with a blocking finding, `needs_revision` requires a blocking finding owned by a revisable stage, and `needs_human_review` requires one owned by human review, so a human-review verdict backed only by advisory findings fails — without claiming to prove any finding correct. Claim-finding binding tests cover a fabricated id, a wrong-platform id, an out-of-range finding index, an exact `(findingIndex, platform, factId)` triple repeated, and a platform-specific finding's binding naming a different platform, while proving the same `(platform, factId)` pair legitimately backing two distinct findings **validates**, and that a `cross_platform` finding may bind more than one requested platform. The id-only accessors `criticClaimRecords()`/`criticClaimTexts()` are proved to expose no verdict, summary, issue, owner, or suggested-action text. Dedicated regressions prove Stage 6's guard on a Stage 5 handoff never rejects what Stage 5 accepts, in both the ordinary-character and the JSON-escaping case, and prove the guard is **exactly** Stage 5's own derived ceiling rather than a larger round number: a caption at the full pipeline cap made entirely of quotation marks — valid under every Stage 5 caption, hashtag, URL and combined provider-visible rule, yet doubling in size once serialized — reaches the injected runner exactly once and is forwarded whole, with a round-trip check proving no truncation or rewriting (`BX19`–`BX21`). `BX18` proves the pipeline caption cap really narrows Facebook's 63,206-character provider limit and that the narrowing is enforced by Stage 5's validator rather than described in a comment. `BX22` proves the derived assembled ceiling for this stage is at or below the shared `MAX_PAYLOAD_CHARS`, so **no structurally valid Stage 6 invocation can reach the shared boundary at all** — the mismatch the earlier record described is gone rather than merely relocated. `BX23` proves the ceiling comes from the one authority and that no measured sample survives in this module. `BX24`–`BX28` prove the other half: an oversized evidence claim is now refused by the evidence contract itself, naming the field and the bound; a maximum-length claim bound on all three platforms is projected **once** as claim text (through `SCRIPT_CLAIMS`) and only as ids per platform, so the per-platform duplication that used to consume the whole payload is gone; that invocation is accepted and reaches the runner exactly once; and the packaging allowance is still measured as a **difference** from `MAX_PAYLOAD_CHARS` against a real assembled prompt rather than encoded as a constant — now provably positive at the maxima, and large enough for a maximal Stage 5 output. **Thirteen deliberate executor mutations confirm those assertions are load-bearing**: the `provisional_pass`/blocking-finding consistency check, the `needs_revision`/`needs_human_review` owner-consistency checks, the requested-platform-sequence check, the exact-triple duplicate guard, the finding-platform coherence guard, the zero-used-claims pre-model refusal, the per-platform claim-binding check, the call to the owning Stage 5 revalidator (bypassed), a validator-authored literal no-approval brand (flipped), the aggregate bound reverted to its original 20,000-character value, the aggregate bound restored to the later hand-measured 100,000-character value (which failed `BX20`–`BX23`), the `PLATFORM_CLAIMS` projection narrowed to a single requested platform (which failed `BU8`, `BX25`, `BX26` and `BX27`), and the dynamic-envelope wording removed from the source (which failed `BX28`). `BX24` asserts a property of the *evidence contract* rather than of this executor, so no executor mutation can exercise it, and none is claimed. The executor was restored byte-for-byte after each. **`final-critic` is merged to `main` through PR #52**, on the same terms as the five stages this paragraph and the ones above it describe: dormant and unreachable. **Determinism is asserted for the validators and boundary under fixed injected runners — real model output is not deterministic and is not claimed to be.**
- `test:postgres` is an opt-in destructive test that requires `PHASE0A_DISPOSABLE_POSTGRES=1` plus a loopback-only `PHASE0A_POSTGRES_ADMIN_URL`. It creates randomly named fresh/upgrade databases, runs the compiled repository migration mechanism (001–007 fresh and 001–004→seed→005–007 upgrade), exercises the startup probe, SQL invariants, concurrent durable decisions, expiry/revocation, exact-package authorization, immutable content-addressed media, migration-007 enforcement/rollback/collision/reapply, and the sanctioned publication entrypoint with only its provider transport replaced by a counting offline stub, then drops only those owned databases.
- `test:http-e2e` requires a migrated, non-default loopback `DATABASE_URL`. It creates durable approval fixtures, proves the compiled production API exits before binding when Render commit identity is absent, then starts it with a fake 40-character Render commit/instance, a randomized test control token, `API_BIND_HOST=127.0.0.1`, a minimal sanitized environment, and a child fetch-deny preload. Real localhost HTTP/TCP requests cover the exact health service/PostgreSQL/commit identity, route authentication, trigger schema/content/body/time bounds, the global incomplete-header deadline, prompt closure of unauthenticated/invalid-content-type partial-body sockets, durable enqueue, CORS, approval capability/review integrity, token non-echo, and security headers. It also covers `POST /console/content-intelligence/preview` against the real bound server: unauthenticated rejection, content-type rejection, empty-goal and unknown-field rejection, the six-stage plan with execution disabled, the evidence-class summary, and — asserted directly against the database — that a preview creates no approval and no brief and leaks no secret. It never takes an authenticated diagnostic branch.
- `dryrun` starts `dryrun.cli.js`, whose only static dependency is the environment-preparation helper. In simulated mode it deletes the fixed Anthropic/image, Slack/console/database, Meta/Google/GBP credential/ID, provider host/version, model-name, and public-base-URL keys and forces `NODE_ENV=test` before dynamically importing any configuration-bearing dry-run/orchestrator module. Injected canned agent, inspected-image, and dummy publication-target fixtures then keep the run offline. It reaches approval-ready state and builds request shapes without approval, network, or publishing, even when the caller began with `NODE_ENV=production`. `dryrun:live` preserves caller environment and does not use those injected seams.
- `test:deployment-controller` feeds reviewed Render CLI JSON fixtures into the production controller's injected boundaries. It also proves the widened bounded readiness window: readiness arriving around 90 seconds still succeeds, absent readiness still fails closed at the bound, a previous instance's `LIVE_SHA` marker never satisfies the target check, and heavy previous-instance log volume neither hides the target marker nor trips the saturation guard, while a genuinely saturated window at the readiness bound still fails closed. It proves literal enable-gate behavior; A-before-B and B-before-A CI completion order; the late current-main/superseded stop; already-current no-deploy; missing/diverged history rejection; exact `LIVE_SHA..TARGET_SHA` migration detection; strict API→worker→scheduler sequencing; and exact health origin/path/identity plus redirect/content/timeout rejection. Health fixtures exercise a real Node 22 byte-stream response, small and exact-4,096-byte success, a canceled one-byte overflow, a lazy synthetic 1-MiB source that supplies only 4,097 bytes, false-small/absent/oversized/malformed Content-Length, multibyte and malformed UTF-8, stream error, abort, and missing/non-byte-stream bodies. The same suite covers Render-CLI-format adjacent/sequential JSON parsing; exact-target worker readiness plus 10-second stabilization; old/missing/malformed/restarted/crash-bearing log rejection; fail-closed Render states; recursive JSON redaction; recognized percent-encoded/mixed and `=>`/`->` fallback forms; and final-summary Markdown/HTML neutralization with bounded output. It invokes neither Render nor a real health URL.
- `check:markdown-links` resolves local Markdown targets offline. `check:env-coverage` compares literal active/test `process.env` reads with `.env.example` while explicitly classifying operating-system-, Render-platform-, and disposable-test-provided variables. `scan:sensitive` inspects tracked active text for high-confidence credential/PII patterns and reports only file, line, and category—not the matched value. These checks need manual triage and do not prove history or external systems contain no secrets.

The in-memory tests do not by themselves verify PostgreSQL concurrency, a live durable hosted-media row/guard query, migration behavior, or HTTP routing. Use the two opt-in integration commands above only against disposable local state. `test:postgres` refuses a non-loopback administrative URL or a run without its explicit destructive-test acknowledgement; `test:http-e2e` refuses a default or non-loopback database. Never point either test at shared or production data. Migration 005 uses a transaction-local 10-second lock deadline and five-minute per-statement deadline; an integration or rollout timeout requires investigation rather than automatic retry.

Offline validation does not verify deployed API authentication/rate distribution, Slack delivery, live model output, real image pixels, public media reachability, provider scopes/tokens, approval-bound account ownership, duplicate prevention, crash recovery, partial publishing, live Render settings, deployment, or external platform behavior.

`dryrun:live`, `/diag/*`, actual API/worker/scheduler processes, shared/production migrations, Slack approval, image generation, and provider calls require explicit authority, an identified non-production environment, controlled test accounts, spend limits, and external reconciliation. There is no provider sandbox assumed by the code. The API/worker/scheduler now require and probe PostgreSQL at startup, so do not launch them with an unidentified `DATABASE_URL`.

The routine offline sequence now also runs `npm run test:payload-mutation` between `test:offline` and `dryrun`, locally and in the CI quality job. Repository validation must also check all Markdown links, active environment reads versus `.env.example`, agent/model/skill loading claims, migration inventory, credential/PII patterns with manual triage, generated/vendor boundaries, whitespace, and the complete diff. The pinned AgentShield scan should run when available, and dependency audit failures caused by unavailable registry/network access must be reported rather than represented as a pass.


## Payload-contract reconciliation — derivation and mutation coverage

**`MERGED` through PR #54 and present on `main`: not established as deployed, not enabled, not production-validated.** The `CC1`–`CC81` group in `test:content-intelligence` covers the reconciliation, and `npm run test:payload-mutation` proves the load-bearing branches. Both are offline: no network, no database, no provider, no credential, and every model call goes through an injected runner.

**What the derivation group `CC1`–`CC38` fail on.**

- **TypeScript and PostgreSQL disagreeing.** `CC1` reads every bound back out of `state/migrations/007_evidence_bounds.sql` and compares it to `EVIDENCE_LIMITS`, naming each mismatch in the check line; `CC2` covers the tag cardinality and per-tag length; `CC3` asserts the migration is additive (no `DROP`, `UPDATE`, `DELETE`, `INSERT`, `TRUNCATE`); `CC4` asserts the rollback drops exactly what 007 adds and removes 007's `_migrations` row; `CC5` asserts the rollback lives outside the forward-only runner's directory and that **neither file claims to have been applied**.
- **The evidence bounds being decorative, or too tight.** `CC6` refuses an over-long claim, subject, attribute, tag, tag list, source ref, provenance, reviewer, superseding id and serialized `detail`; `CC7` accepts a record at every bound *exactly*, so the bounds cannot invalidate the widest legitimate record; `CC10` asserts the one production write path still runs `assertValidEvidenceRecord`.
- **The escape factor being hopeful.** `CC8` refuses a control character and an unpaired surrogate in evidence text — the only code units `JSON.stringify` expands sixfold — and `CC9` measures the worst *serializable* character and requires it to cost exactly `MAX_JSON_ESCAPE_EXPANSION`.
- **A producer's maximum exceeding a consumer's guard.** `CC11`–`CC13` are executed proofs, not constant comparisons: they build an output at every field maximum and every array maximum at once, with maximum-length evidence ids, through each owning stage's real validator, then measure what it serializes to. `CC13` walks all nine adjacent producer/consumer pairs and requires the consumer's guard to be **exactly** the producer's derived ceiling and the producer's real maximal output to fit it, naming any broken pair. If a shape witness in `payloadContract.ts` stops matching what a validator actually returns — a renamed field, a new channel, a wrapper object — these fail.
- **A hand-maintained aggregate replacing the derivation.** `CC14` fails if any stage module writes a derived ceiling as a literal; `CC15` requires all six to import from the authority; `CC16` requires `MAX_PAYLOAD_CHARS` to be computed from the assembled ceilings; `CC17` requires every handoff guard to equal its producer's ceiling and every assembled ceiling to fit the shared boundary; `CC18` requires the escape factor and one-token-per-UTF-8-byte worst case to have exactly one definition each.
- **An output contract outgrowing its budget.** `CC19` requires every stage's escaping-aware serialized UTF-8 byte ceiling to fit its policy's visible-output token budget; `CC20` requires every stage policy to be budgeted; `CC21` requires configured budgets to equal derived floors; `CC22` requires the one-token-per-byte worst case, explicit disabled-thinking policy, and every derived budget to remain within the centrally recorded 128,000-token model cap. No prose-density assumption remains. `CC36` exercises the production runner and SDK request seam and requires the exact Anthropic request to carry `thinking: { type: "disabled" }`, the resolved Opus 5 model and derived `max_tokens`, with no tools, while proving a legacy `runAgent` request still omits thinking.
- **The narrowing being undocumented or accidental.** `CC23` requires the pipeline caption cap to be below the largest provider limit, to be documented as a deliberate narrowing, and to dominate every provider policy; `CC24` requires Google Business Profile to keep its tighter 1,500 — the pipeline cap narrows, it never widens.
- **Anything becoming reachable, or a credential mattering.** `CC25` requires all six stages to remain `executionEnabled: false`; `CC26` requires the authority module to import nothing and to contain no network call, module load, environment read or provider name in its code; `CC27` sets a **nonempty `ANTHROPIC_API_KEY`**, executes a stage, and requires the injected runner to receive the request, the preview to stay inert, and the variable to be restored; `CC28` requires no stage module to construct the provider runner or read the credential.
- **The review findings returning.** `CC29` pins the 3,991-byte compact JSON / 4,810-byte canonical JSONB counterexample and requires TypeScript to reject it; `CC30` exercises the relation-note bound including a multibyte edge; `CC31` builds 64 valid same-subject/same-attribute records through the real pack builder, observes all 2,016 conflicts, and requires both the real renderer and shared executor boundary to fail closed with zero runner calls; `CC32` proves `maxProjectedRecords` is executed by the builder; `CC33` accepts multibyte output only while its UTF-8 bytes fit and rejects valid-text adversarial input that defeated the former prose ratio; `CC34` pins collision-safe plain `CREATE`; `CC35` pins NULL-tag rejection plus character/byte tag parity. `CC37` accepts an exact 4,000-byte canonical detail, rejects 4,001, and pins the signed `-5e-324` numeric expansion at 327 bytes through the exact counterexample for which the former 326-byte numeric allowance estimated 4,000 bytes while PostgreSQL canonical JSONB is 4,012 bytes. `CC38` requires record validation at pack construction and the shared pre-model boundary, rejects overlong and control-character records with zero calls, and still builds 64 maximum valid conflicting records without truncation.

**Semantic pack integrity, and the request policy.** Three groups were added after review found them missing. `CC47`–`CC69` execute a stage against a deliberately malformed pack and require **zero runner calls** for each: a valid `creative_hypothesis` hand-placed in `allowedFacts` beside a valid business fact; an active verified fact whose `reviewBy` predates the pack's own `builtAt`; a record in a section its kind does not permit; one record in two sections at once; lifecycle wrong in both directions; a malformed `builtAt` in three shapes (not a date, a calendar date rather than an instant, and a value that does not round-trip); the reviewer's exact conflict counterexample (invalid `basis`, fabricated ids, a 50,000-character subject, an over-long note); a control character in a conflict field; an over-long conflict id; a conflict naming one id on both sides; a conflict outside canonical order; a repeated pair; a conflict endpoint held in a section other than `conflictedEvidence`, which covers both a conflicted record left citable and one parked in a different exclusion list; and counts that are dishonest, unknown, missing, or non-integer. A control case proves the unmodified pack still executes exactly once, so every refusal is attributable to its mutation rather than to the fixture. Each check executes the stage rather than calling the validator directly — a check that only called the validator would pass even if no boundary invoked it, which is exactly how `evidencePackInvariants` detected hypothesis promotion for five phases while no stage boundary ever called it.

`CC39`–`CC46c` cover the provider request policy, and **two separate bounds** because the SDK implements only one. The stage boundary sends `maxRetries: 0` explicitly (against an SDK default of 2, which made "exactly one model request" describe one wrapper invocation and up to three wire attempts). The legacy agent boundary keeps SDK-default retries and its original 90-second non-streaming budget, asserted in the same breath so the separation is deliberate rather than incidental. The SDK's `timeout` option arms a timer around the underlying `fetch` and clears it in a `finally` when that resolves — for a streaming request, when response headers arrive — so it is asserted as a small `requestSetupTimeoutMs` and the total `streamDeadlineMs` is asserted separately, equal to `stageStreamDeadlineMs` of that stage's own budget for every policy, and strictly larger than the setup timeout. An injectable stream-and-timer seam then proves the deadline is *enforced*, not merely computed: a completing stream returns, opens exactly one stream, is never aborted and has its timer cleared; a stream that never finishes is aborted at the deadline, reported as a named `StageStreamDeadlineError`, opens exactly one stream, takes no retry, and has its timer cleared on that failure path too; and a failure for any other reason keeps its own reason rather than being relabelled a timeout. No test waits the real 35–67 minutes. Source checks confirm the production runner streams and reassembles with `finalMessage()`, arms and clears its own deadline, resolves as the registry's production runner, names the two bounds apart, and carries no 90-second literal anywhere on the stage path. `CC43` requires the throughput floor to be documented as an explicit operational assumption and to claim no observed model rate; `CC43a` compares the derivation comment's budgets against `POLICY_MAX_TOKENS`, so a stale figure cannot survive a budget change.

**Positive controls for conflict packs.** `CC70`–`CC81` exist because the whole invalid-conflict group passed while the validator was *also* rejecting every legitimate conflict pack the builder produces — refusal coverage cannot see that, and only a positive control can. Both builder routes are exercised: an ordinary **inferred** fact conflict, and a **declared** conflict over non-fact evidence built through the real relation path. Each must be accepted by the validator, the renderer, preview/context construction, and an eligible stage invocation; each must hold both endpoints as real records in `conflictedEvidence`, absent from every usable section and present in `unusableEvidenceIds`. A further check proves the distinction the acceptance does not blur: citing a conflicted endpoint against a *valid* pack is still refused, by output validation after the stage's one legitimate request, rather than by the zero-call pre-model pack refusal. `CC74`–`CC75` prove every conflict's `aClaim`/`bClaim`/`subject` are exact snapshots of the records it names and that the ids `conflictedEvidence` holds are exactly the ids the pairs name — the equivalence that lets the projection carry the pairs alone. `CC76`–`CC79` refuse a fabricated `aClaim`, `bClaim` or `subject` and a conflicted record no conflict names; `CC80`–`CC81` refuse a conflict `subject` and `note` that fit the code-unit allowance but exceed the same allowance in UTF-8 bytes.

**The mutation harness.** `npm run test:payload-mutation` applies thirty-three focused mutations across nine files (`payloadContract.ts`, `modelPolicy.ts`, `finalCritic.ts`, `packagingAdaptation.ts`, `stageExecution.ts`, `evidence/pack.ts`, `evidence/contract.ts`, `sdk.ts`, and `007_evidence_bounds.sql`), rebuilds, and requires the named owning checks to fail; a crash is not a pass, and neither is a mutation that fails to compile. It restores every file byte-for-byte by SHA-256 and requires a final green rebuild, restores on `SIGINT`/`SIGTERM`/`SIGHUP` as well as on normal exit, and refuses any mutation whose site is missing or non-unique. The original ceiling, migration-parity, guard, policy, caption, and record-bound mutations remain (the record mutation removes actual builder cardinality enforcement and must fail `CC32`). The earlier review mutations remove conflict-cardinality enforcement (`CC31`), substitute compact JavaScript detail length (`CC29`), loosen relation-note validation (`CC30`), remove output UTF-8 byte enforcement (`CC33`), accept NULL tags (`CC35`), restore helper overwrite authority (`CC4`, `CC34`), replace disabled thinking with adaptive thinking (`CC36`), undercount signed JSONB numeric expansion (`CC37`), or bypass builder record validation (`CC38`). The semantic and request-policy mutations stop the executor boundary calling the pack validator (`CC56`, `CC67`), drop the `allowedFacts` kind restriction (`CC48`), stop checking `allowedFacts` for citability at `builtAt` (`CC49`), drop the per-field bound on conflict fields (`CC55`, `CC56`), stop comparing `counts` against the sections they describe (`CC62`), let a conflict endpoint live in any section again rather than `conflictedEvidence` (`CC61`), stop the builder routing conflicted records into `conflictedEvidence` at all — the builder half of the defect, which invalidates the legitimate packs the positive controls build (`CC70`, `CC73`) — compare a conflict's claim snapshot to itself instead of to the record it names (`CC76`), drop the UTF-8 byte allowance on conflict fields (`CC80`, `CC81`), restore the SDK's default of two retries (`CC39`), pin the stream deadline to the old 90-second value instead of deriving it (`CC41`, `CC42`), conflate the request-setup timeout with the total stream deadline (`CC41`), stop aborting a stalled stream when the total deadline expires (`CC45`), and never clear the deadline timer (`CC44`). The restored suite passes after all thirty-three.

**The constraints themselves run in the PostgreSQL job.** `test:postgres` applies migrations 001–007 on randomly named disposable PostgreSQL 16 and 18 databases. It rejects every over-bound text field, all-NULL and mixed-NULL tag arrays, the exact 3,991/4,810 detail counterexample, an exact signed `-5e-324` 4,012-byte counterexample, and an over-bound relation note; canonical detail at exactly 4,000 bytes inserts and 4,001 is rejected. A record at every boundary still inserts, and a direct malformed durable row is rejected during TypeScript reconstruction. The suite then runs the documented 007 rollback and compiled reapply. The upgrade path seeds an unrelated function at 007's exact helper signature, proves migration 007 fails without overwriting it or recording itself, removes only that disposable fixture, and proves 007 then applies. PostgreSQL 16.15 and 18.6 each report **208 checks** (fresh 59, upgrade 80, durable 69).

**What this coverage does not establish.** It does not validate any bound against a real model, a real workload, or a populated production table. The token proof is a worst-case serialized-byte bound, not an observation of a provider tokenizer; no tokenizer or provider is invoked. Migration 007 and its rollback have **not** been applied to production — all SQL coverage uses disposable databases. Production evidence is the operator's 2026-09-02 aggregate read-only audit, not a test result.

## GitHub CI behavior

`.github/workflows/ci.yml` runs on pull requests to `main`, pushes to `main`, and manual diagnostic dispatch. The Node 22 quality job runs the locked install, build/typecheck, `npm run test:offline` (currently the eight canonical offline self-test commands listed above), simulated dry run, controller fixtures, repository checks, whole-tree whitespace check, and production dependency audit; it never invokes `dryrun:live`. A two-version disposable matrix reuses `test:postgres`, then migrates a dedicated database and runs `test:http-e2e` on PostgreSQL 16 for established compatibility and PostgreSQL 18 for production parity. AgentShield 1.4.0 is a separate required job: a nonzero scan exit fails CI, and its JSON report upload uses `if: always()` so evidence is retained after a scan failure. Workflow validation downloads actionlint 1.7.12 from its pinned release, verifies its archive checksum, runs it against every workflow, and parses checked-in YAML.

CI has no production secrets. Its only database uses loopback credentials inside the disposable runner service, and the HTTP self-test denies outbound fetch. The production deployment workflow triggers only from a successful push-originated `CI` run on `main`; manual CI dispatch cannot deploy. See [Deployment control](DEPLOYMENT.md).
