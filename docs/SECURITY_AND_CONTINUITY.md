# Security and continuity

## Current trust findings

- `/triggers`, `/diag/ig`, `/diag/gbp`, and `/console/*` now fail closed unless the same transitional `CONSOLE_TOKEN` is supplied as `Authorization: Bearer` or `x-console-token`. Query credentials and wildcard CORS were removed. The fixed-window limits are in-process and keyed by direct socket address, so they are neither distributed nor user-aware and may become service-wide behind a proxy.
- Trigger input is restricted to an exact bounded `goal` object. Caller-supplied `approvedFacts` and unknown fields are rejected, and the orchestrator independently replaces any internally supplied facts with the checked-in canonical file.
- Header receipt and the complete HTTP request receive phase use 10-second deadlines checked at one-second intervals. Trigger/approval body readers independently enforce 16-KiB/10-second bounds, while early body-bearing authentication or content-type rejections close the socket without draining unread bytes.
- Approval remains a token-bearing URL transported through Slack/browser history. PostgreSQL stores only the token's SHA-256, and the decision token expires, but there is still no authenticated approver identity or safer review session. UUID-shaped review/decision requests consume a process-local 300/minute direct-socket bucket plus a 30/minute direct-socket-plus-approval-UUID bucket; neither is distributed or identity-aware, and the global bucket can become service-wide behind a proxy.
- Every environment running worker approval delivery requires a validated exact HTTPS `hooks.slack.com/services/...` webhook before approval creation; blank is valid only when that flow is not run, such as direct offline `createApproval` tests. Production additionally validates a public root HTTPS review origin. Delivery refuses redirects and uses bounded retry with a 10-second timeout per attempt. Failed/uncertain notification triggers bounded revocation and must end in confirmed revocation; inability to confirm raises a composite error for reconciliation and returns no approval handle. Escalation notifications remain best-effort and do not grant authorization.
- Slack package summaries are model-influenced previews. Each line is bounded, sanitized, and then code-delimited so controls, mrkdwn, links/bare domains, mentions, and backticks remain inert. The separately generated URL is labeled as the authoritative exact-payload review and is the message's sole active link; operators must decide from that hash-verified page, not preview text. Best-effort escalation messages apply bounded inert-text treatment to trigger/model-authored goal, reason, and run ID.
- Approval authorization is bound to the canonical SHA-256 of the immutable exact provider array, including destination and media digest. The complete social-post subject must be nonempty, strict-valid item by item, and unique by platform; code enforces that before creation and decision, on durable load, and throughout publication. Status, matching durable decision, subject type, authorization expiry, revocation, index, exact payload, runtime account/location/host/version parity, and live immutable media bytes are checked through a module-issued `PublicationGuard.beforeMutation()` immediately before every provider HTTP attempt, including read-only status polls and all retries/multi-step requests. The callback name is transitional; its boundary is all provider I/O. A caller-forged/no-op guard is rejected; a provider that does not await the issued guard at every request is inadmissible. Request construction also enforces target parity. All native provider requests refuse redirects, and a 2xx publication response without a post ID is failure. There is no autonomy/boolean bypass. The revocation primitive has no HTTP/operator UI.
- The default Instagram-login token and original environment seed are persisted plaintext in `session_state`; the alternate Facebook-login token remains environment-managed and is not refreshed by this module.
- The worker avoids unrelated refresh calls: its Instagram refresh timer starts only when Instagram is active, and post-approval Instagram/Google token acquisition is gated by the exact approved platforms. This reduces credential-bound calls but does not isolate or encrypt the environment credential object.
- Credential-bearing native provider requests, Meta/Google diagnostics, Instagram refresh, Google OAuth exchange, and Slack requests refuse redirects. Instagram host selection is exact-allowlisted to `graph.instagram.com` or `graph.facebook.com`; other provider endpoints are fixed/validated by their call sites. This reduces credential replay risk but does not verify external provider ownership or behavior.
- Provider errors may include response text in logs/database outcomes. Review redaction before broad log access.
- Passing generated media is intentionally public and cached for one year. New URLs bind UUID plus SHA-256, and the guard before every provider HTTP attempt recomputes the immutable stored byte digest and revalidates those bytes under the 5-MiB/JPEG/allowed-profile policy; the UUID-only route remains readable for already-published links. Migration 005 blocks media id/MIME/bytes/digest changes and every DELETE, creating a deliberate integrity/retention tradeoff. Model-supplied URLs/provenance, missing generation/hosting setup, fal URLs with credentials/fragments/nonstandard ports or redirects, oversized/non-image bytes, garbled text, identifiable people/features, readable plates/VIN/contact/customer documents, unsafe shop practices, material misrepresentation, QC errors, and malformed strict QC responses fail closed before approval. The same gate runs after every critic-requested image revision, and failed bytes are not intentionally hosted.
- Production rejects injected orchestration runner/image-resolver/publication-target seams and injected vision inspectors. These hooks are restricted to non-production offline validation; simulated dry-run scrubs the sensitive environment and forces test mode before configuration-bearing imports.
- The `/console/*` control-plane gate **did not drain an unread request body on auth or rate-limit failure** until PR #40 (`4891bf3`). A rejected body-bearing request left declared-but-unsent bytes on a keep-alive connection, so a pipelined follow-up could be consumed as the first request's remainder. The fix is **merged and deployed on the API** (independently verified 2026-08-28 by a separate final-inspection session: unauthenticated `/console/manifest` returns `401` with `WWW-Authenticate: Bearer` at the deployed target), which is the only service exposing those routes, so the exposure is closed in production. It requires no credential to attempt, but reaches only the console routes and cannot bypass authentication.
- Interruption **recovery** is **merged in PR #36 (`0828cc9…`) and deployed**: exclusive worker ownership via a session-level advisory lock, durable phase markers committed before each side effect, and refuse-don't-resume terminalization of interrupted briefs. The worker was independently verified running the current target on 2026-08-28; the behavioural bootstrap evidence itself (the ~58-second ownership wait, the August 10 reconciliation) remains **operator-reported 2026-08-27** and was not re-examined in that inspection. Publication **idempotency** remains absent in every case — recovery makes an uncertain provider outcome visible and refuses to retry it, but cannot prove whether a post was created, and provider-level `withRetry` can still reissue a request after an ambiguous network outcome. Duplicate publication is not prevented.
- A human approval can be orphaned by worker interruption: the reviewer approves, no process is waiting, nothing publishes, and nothing alerts. That is an approval-integrity failure, not merely an availability one. **The recovery and startup sweep that close it are deployed**: recovery revokes an interrupted brief's approval, and a startup sweep revokes pending approvals with no owning brief marker, so an orphaned link cannot later authorize a publication nobody is waiting for.

Residual risk remains and is not closed by that deployment. A brief interrupted between the reviewer's approval and the next worker start is still stranded for that interval — recovery is a sweep at startup, not a live watcher — and the reviewer receives no alert during it. Recovery also terminalizes rather than resumes, so an approved package that was never published must be reissued as new work. Publication idempotency is still absent, an ambiguous provider outcome is still only *visible* rather than resolvable, and approval review still rests on a bearer URL with a generic `human` identity rather than an authenticated reviewer.
- Ownership is mutual exclusion, not a fencing token: if the owner's connection dies while its process lives, PostgreSQL releases the lock and a successor may reconcile concurrently. The Phase 0A publication guard is the fence — the successor's revocation makes the old process's next pre-request authorization check fail closed — which narrows the exposure to a single already-in-flight HTTP request, itself classified as an uncertain outcome.
- The Phase 0B.1 `strategy-concept` executor treats the goal and the evidence projection as **untrusted data**, wrapped in labelled delimited blocks that say so. Prompt-injection framing is a mitigation, not a control: the controls are that the boundary registers no model tools, refuses any capability the registry did not declare (only `read_evidence_pack` is declared), builds instructions solely from allowlisted registry assets, and binds every cited evidence id to the pack the caller built rather than trusting the model's classification. **Prompt and skill assets do enter the instruction channel; factual reference assets never do** — `strategy-concept` omits its declared `config/approved-facts.json` reference so the classified evidence projection stays the single factual input, and asset metadata records the channel each asset actually reached.
- The Phase 0B.2 `automotive-truth` executor — **merged through PR #44, deliberately dormant, and not established as deployed or production-validated** — inherits all of the above and adds one control specific to a stage that decides what may be asserted: **a permission is a binding to an evidence id, never a sentence the model wrote.** The class the evidence system recorded overrides the model's declaration, and what may be claimed is read back from the records. Because a skill entering the instruction channel becomes de facto authority, the skill registered for that stage was audited before use: `skills/compliance-checklist/SKILL.md` is the orchestrator's existing `brand-compliance-critic`'s publishing-era rubric and states concrete facts, so it was removed from this stage — it remained registered on `final-critic` at the time — and replaced with `skills/claim-boundaries/SKILL.md`, which is claim-scoped and asserted by test to contain no facts of its own. Phase 0B.6 later removed the checklist from `final-critic` too, for the same reason; it is now registered on no target stage, though it remains unchanged for the orchestrator's own critic. **No new unverified automotive fact was introduced in any prompt or skill.**
- **The id binding does not extend to prose.** Phase 0B.1's `angle`/`concept`/`rationale` are length-bounded and not checked for meaning, so a response can misstate a performance correlation as fact in prose and still validate. Phase 0B.2 receives the complete typed Stage 1 result — including hypotheses, assumptions, and typed citation arrays — as bounded untrusted data, then creates only a structural whitelist of ids from the classified evidence projection. It does not semantically prove any prose or restatement true. Only exact claims read back from evidence records through the typed accessor are permitted. Phase 0B.3's `hook-story-script` executor — **merged through PR #46, deliberately dormant, not enabled, and not established as deployed or production-validated** — narrows the surface again: its factual input is derived exclusively from the ids stage 2 permitted, the complete pack is never rendered as an alternate claim source, and both prior-stage outputs are revalidated against the pack rather than trusted through their branding. Its style skill was audited on the same reasoning as stage 2's: `skills/brand-voice/SKILL.md` carries a founding year, a locality, a street address, a registered slogan, makes, and CTAs, so it is not injected here — it is preserved for its existing consumers — and `skills/script-craft/SKILL.md` replaces it with craft-only guidance asserted by test to contain no facts. The rejected `agents/copywriter.md` placeholder also declared tools; the replacement prompt declares `tools: []`. **What none of this does is check meaning.** Deterministic validation cannot prove a paraphrase faithful to the fact it cites or detect every uncited factual implication. No executor logs prompt, strategy, evidence, model text, or secrets. All three are **dormant** — nothing in production reaches them — so this describes source boundaries, not a running system.
- Phase 0B.4's `production-direction` executor — **merged through PR #48, deliberately dormant, not enabled, and not established as deployed** — narrows the surface once more: its factual projection is derived exclusively from the claims stage 3 actually used, so neither stage 2's wider whitelist nor the evidence pack reaches the model, and stage 2's prose is supplied to the validator only. Its skill was audited on the same reasoning as the earlier stages': `skills/image-brief/SKILL.md` carries brand assets, exact hex colours, the registered slogan, platform feed profiles, provider and model routing, hosting and QC rules, so it is not injected here — it is preserved for the existing image flow — and `skills/production-craft/SKILL.md` replaces it with craft-only guidance asserted by test to contain no facts. The rejected `agents/image.md` placeholder also pinned a model and declared tools; the replacement prompt declares `tools: []`.
- **A visual stage adds limits a textual one does not have.** Deterministic validation cannot prove that a shot represents reality, that a requested asset exists or may lawfully be used, that anyone consented, or that an action is physically safe. Stage 4 therefore expresses every production and asset need as a **requirement branded `availabilityVerified: false`**, never as an assertion that the thing exists, and brands its direction **non-executable** as well as unverified and non-publishable — nothing may hand it to a generator, a camera, or a publishing path as an instruction. It generates, downloads, inspects, hosts and stores nothing, selects no provider or model, and returns no URL, digest, QC result, or approval state.
- Phase 0B.5's `packaging-adaptation` executor — **merged through PR #50, deliberately dormant, not enabled, not established as deployed, and not production-validated; production evidence: none** — keeps the same boundary rather than widening it at the point where widening would be easiest. Merge changed repository state only. Captions adapt the *script*, so the factual authority stays the claims stage 3 actually used: stage 4 is supplied as creative and production context only, its narrower visual selection does not remove a claim the script used, and neither stage 2's wider whitelist nor the evidence pack reaches the model. All three prior outputs are revalidated against the same pack before any model call. Two of its four registered placeholder assets pinned concrete models, declared tools, and described separate subagent calls against runtime briefs, analytics readouts, provider payloads, destinations and CTA URLs; `skills/platform-specs/SKILL.md` mixes format guidance with payload construction, `ACTIVE_PLATFORMS` state, scheduling and publication behaviour; and `skills/local-seo/SKILL.md` states a street address, city lists, makes and services the stage 3 claim set may not establish — injecting it would let the stage reacquire factual authority from a keyword file. All four are preserved unchanged for their existing consumers; `agents/packaging-adaptation.md` declares `tools: []` and pins no model, and `skills/adaptation-craft/SKILL.md` is craft-only, asserted by test to contain no facts.
- **The stage that most resembles publishing must most visibly refuse to be it.** Dedicated provider, payload, media, destination, account, location, CTA, URL, approval, publication, scheduling, and API fields are structurally absent. Its per-platform limits and single hashtag tokenizer are **imported from `packageMap.ts`**, the module that already enforces them on real provider text, so no competing policy or parser exists: caption prose may contain no hashtag token, canonical tags live only in their array, and the combined caption/separator/tags must fit the provider-visible limit. The provider-payload `Platform` union's `gbp` and this stage's `google_business_profile` remain distinct but are bound by exhaustive forward and inverse `Record` maps plus both runtime round trips, so changing either union requires a reviewed mapping change. One reusable guard rejects recognizable URL syntax — explicit URI schemes and `www.` tokens — in captions, local keywords, open questions, and claim-use summaries; it does not claim to detect obfuscated destinations or semantic references. Its recommended time is branded `timingVerified: false` **and `schedulable: false`** and is a bounded `HH:MM ET` note carrying no date and no timestamp, so its shape refuses to become a scheduler instruction. Deterministic validation still cannot prove a caption faithful to the script, a shortening meaning-preserving, or a hashtag, local keyword or time relevant, useful or truthful — that gap is contained by branding, not by semantic understanding.
- Phase 0B.6's `final-critic` executor — **`IMPLEMENTED` in source on draft, unmerged PR #52; not enabled, not established as deployed, and not production-validated; production evidence: none** — narrows the boundary one stage further than 0B.5's. Its factual surface is not stage 3's whole used-claim set but stage 5's own typed, per-platform claim bindings, read only through stage 5's evidence accessor; neither stage 2's whitelist, the evidence pack, stage 4's direction, nor stage 5's captions ever become an alternate claim source. All four prior outputs (stages 2–5) are revalidated against the same pack before any model call, including the requested-platform sequence checked against stage 5's own package sequence exactly. Three registered assets were audited and rejected on the same reasoning as every prior stage's: `agents/brand-compliance-critic.md` and `skills/compliance-checklist/SKILL.md` are the *other*, currently-running critic's contract and rubric — a pinned model, `tools: Read, Skill`, a runtime `brief.approvedFacts`, exact provider payloads, and a legacy-subagent routing field, none of which exists here — and `config/approved-facts.json` is GCD's raw fact reference, already the evidence system's classified source. All three are preserved byte-for-byte for the orchestrator's existing critic; `agents/final-critic.md` declares `tools: []` and pins no model, and `skills/critique-discipline/SKILL.md` is craft-only, asserted by test to contain no facts and to name no legacy subagent.
- **The most consequential control in this stage is what it cannot become.** Its assessment carries five literal-`false` fields — `authoritative`, `approvalGranted`, `publishable`, `executable`, `productionValidated` — asserted by the validator and never copied from the model; the output contract has no field through which a model could set any of them, so a wrongly optimistic model output that tries to smuggle in an approval claim is refused outright, not silently dropped. `verdict` and `requiresHumanReview` are checked for structural self-consistency against the findings actually returned — a verdict cannot claim blocking findings with none marked blocking, or the reverse — but this is not a correctness check: nothing here proves a finding is actually right. It does not replace the existing `brand-compliance-critic` gate the live orchestrator runs against the real provider payload, which is unchanged. This stage is dormant and unmerged: nothing in production or in `main` reaches it.
- Production PostgreSQL external access remains open to `0.0.0.0/0` — independently reverified 2026-08-28 by a separate final-inspection session. This is a recorded high-priority, separately authorized follow-up, not changed by Phase 0D or by this rollout; do not combine networking remediation with deployment-controller cutover without a separately reviewed plan.

## Deployment authority and credential boundary

Independently verified on **2026-08-28**: native auto-deploy is off on the API, worker, and scheduler, and `RENDER_DEPLOY_AUTOMATION_ENABLED` is `false` — intentionally leaving zero unattended deployment authorities until controlled proof. The GitHub `production` environment's five non-secret variables were last verified 2026-08-24 and were **not** revisited; reconfirm rather than assume. Enabling that gate is now **eligible** — the ownership fix is deployed and the Phase 0B.0 rollout is complete, so the condition that previously blocked it no longer holds — but it remains **separately unauthorized and disabled**, and turning it on requires its own authorization plus immediate re-verification. Do not re-enable native Render auto-deploy while the GitHub controller is enabled. The production workflow is serialized and read-only at the repository boundary. A non-environment job verifies the intended workflow name, exact successful `push` event, `main` branch, same repository name/ID, event-supplied target SHA, and literal repository gate before the dependent job can enter the GitHub `production` environment. After the concurrency wait, the controller also requires that target to remain current main before any Render command. PRs, forks, dispatches, issues, comments, and artifacts cannot supply the automatic production target. The production job receives only the Render API key plus non-secret service/workspace/health identifiers. The key must never enter Render application environments, artifacts, summaries, or repository content.

The controller blocks migration-bearing releases before any service or migration action and makes one deployment attempt per service. For bounded failure evidence, it first recursively redacts values under exact case-insensitive secret-bearing keys in valid JSON, then applies defensive fallback redaction only to its reviewed mixed/escaped and credential patterns. That fallback recognizes the explicit secret-key policy with `:`, `=`, `=>`, and `->` separators plus supported percent-encoded or mixed authorization, webhook, credential URL, database/cache URL, and key/value forms. Percent-decoding occurs only in bounded private detection shadows; on a recognized hit the original diagnostic is replaced wholesale, and decoded text is never emitted. Every untrusted field is numeric-entity encoded through one Markdown-inert `<code>` rendering path before it reaches the step summary. These layers lower accidental disclosure and markup-activation risk but neither prove coverage of arbitrary future secret syntax nor make production logs public-safe; no raw log artifact is created. Interactive Render MCP reads remain permitted for diagnosis, but MCP availability grants no write authority.

## Sensitive tracked content

`config/approved-facts.json`, brand/agent/skill content, and the master prompt intentionally contain public business identity, service, address, phone, claims, and a public booking capability URL. These are executable content inputs, not credentials. Their continued publication and accuracy require business-owner review; do not mechanically redact them and silently change marketing behavior. Personal contact fallbacks and production platform numeric IDs do not belong in current setup documentation.

The credential-history pattern scan matched an embedded raster inside a brand SVG, not a provider credential, after manual triage. Pattern scans remain incomplete evidence.

## Secret handling

Keep database URLs, Anthropic/fal keys, Slack webhooks, Meta/Google tokens and secrets, console tokens, approval tokens/links, database exports, private analytics, and unpublished packages out of Git, chat, issues, screenshots, and fixtures. A SHA-256 token digest protects at-rest disclosure but does not make the URL safe to share. Base64, hashes of low-entropy secrets, and embedded JSON are not encryption.

If a credential is discovered in current files or history, revoke/rotate at the provider first, assess logs/use, then make a coordinated history-cleanup decision. Never rewrite history as an incidental documentation change.

## Private continuity register

Maintain outside Git: business/technical/social/approval/billing owners; Render team/services/domains/deploy controls; database owner/backup retention/restore evidence; every secret's storage location and last rotation; provider tenants/apps/pages/accounts/locations/scopes/test assets/revoke paths; Slack channel/webhook owners; Arcade counterpart; public site/booking ownership; incident contacts and vendor support routes. Record locations/owners, never values.

## Takeover

1. Obtain authorized source, Render, database backup, Slack, Anthropic, fal.ai, Meta, Google, Arcade, DNS/domain, and website/booking access.
2. Suspend scheduler/worker until platform identity, active platforms, autonomy phase, queue, approvals, and external posts are reconciled.
3. Rotate one boundary at a time, update all consuming services, and validate with offline/read-only checks. The API and Arcade currently share `CONSOLE_TOKEN`; update both before resuming.
4. Move persisted tokens to an encrypted/managed store before relying on long-term unattended operation.
5. Restore into isolation, then reconcile every external post/message/cost after the backup timestamp before resuming.

## Phase 0A rollout history and future migration safety

`005_approval_integrity.sql` adds approval fields/constraints, append-only decisions, guarded status/revocation transitions, and media byte hashes/immutability. It deliberately revoked legacy pending/approved rows without a canonical hash/token hash, cleared and constrained the plaintext-token column to NULL, required nonnull equal legacy/canonical copies for bound Phase0A subjects, and backfilled media digests. During its production rollout, concurrent native auto-deploy started the worker before the API pre-deploy migration completed; the worker failed twice on the missing `approval_decisions` table and recovered after migration. That event is the empirical basis for Phase 0D serialization. Future migration releases must still stop before unattended deployment; stop worker/scheduler; take a backup; drain or revoke old/incompatible approvals; assess actual table/media volume, data, locks, and deadlines; use exactly one migration runner/process; start only compatible services after success; and issue fresh approvals. A timeout or partial rollout is a stop-and-investigate condition, never an automatic retry signal.

## Decisions required

Replace the shared control secret with scoped authenticated identities and distributed trusted-proxy-aware limits; redesign approval identity/token transport and expose authorized revocation operations; encrypt provider tokens; implement durable publish idempotency and provider reconciliation; design a reviewed media-retention migration plus other data/event/approval retention; complete the reviewed cutover from checked-in facts to the Phase 0B.0 evidence substrate while preserving source/confidence/freshness/last-review controls; and assign named owners plus restore-test cadence.
