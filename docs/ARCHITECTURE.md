# Architecture

Verified against source on 2026-08-26. Current Render, health, migration, database-count, inventory, control, and error state was **independently verified by a separate final-inspection session on 2026-08-28** — see [Status](STATUS.md) for the exact rows and their freshness. Phase 0A and Phase 0D are deployed. Phase 0B.0, the foundation slice of Phase 0B, is merged and deployed; the six reasoning stages themselves are not yet wired.

**This document describes two things and labels which is which.** *Current production runtime* is what the live release does. *Current source / next production release* is what `main` does and what the next release will do. The last independently verified production release is `44d7336…` (2026-08-28), and its behavior — PR #36's worker ownership protocol, PR #38's media normalization, and Phase 0B.0 (migration 006 applied, the evidence tables present and empty, no reasoning stage executing) — is current production behavior. The source lineage additionally carries the **merged but dormant** Phase 0B.1 `strategy-concept`, Phase 0B.2 `automotive-truth`, Phase 0B.3 `hook-story-script`, Phase 0B.4 `production-direction`, Phase 0B.5 `packaging-adaptation`, and Phase 0B.6 `final-critic` executors — **all six are now on `main`**. None is enabled, established as deployed, or production-validated, and because nothing calls any of them, the difference changes no production behavior. For the exact current `main`, read Git; this document does not track it. The earlier PR #36 behavioral bootstrap (the ~58-second ownership wait, the August 10 account-history reconciliation) and PR #38's controlled brief remain **operator-reported from 2026-08-27** and were not re-examined by that inspection. See [Status](STATUS.md) for durable merge identifiers and dated production evidence, and [Roadmap](ROADMAP.md) for the cursor.

## Product architecture principle

The destination is the GCD Content Intelligence Platform / Content OS, not merely a scheduler. It should build an organic automotive media engine for qualified reach, followers, repeat viewing, affinity, retention, engagement, GCD authority, and local dominance, with attribution, leads, and revenue optimized later. Humans continue to film; external editing remains the V1 production path.

> Research gives us the prior. GCD empirical performance becomes the posterior.

Agents should reason. Deterministic services should retrieve, validate, mutate, store, enforce, and publish. Evidence collection may be automatic; changes to prompts, skills, production process, agent behavior, or publishing rules remain governed. Automotive truth, safety, privacy, and approval integrity are hard rules. The system must never fabricate diagnosis, failure/repair evidence, customer facts, or shop evidence, and should prefer real GCD evidence over generic decoration.

## Current production architecture

## Runtime ownership

The repository root builds one TypeScript project into `dist/`. Render declares four runtime resources: PostgreSQL, an API service, a worker service, and a daily cron process. The API and worker share PostgreSQL; the scheduler writes only to the brief queue. All three process entry points initialize state with `requireDurable: true`, which requires `DATABASE_URL`, a successful PostgreSQL connectivity query, the migration-005 approval/media columns, both probed approval integrity constraints, and all four approval/media integrity triggers before work begins. In production, the API and worker also require Render's platform-injected full `RENDER_GIT_COMMIT`; `RENDER_INSTANCE_ID` is retained as optional non-secret correlation identity. In-memory state remains an explicit offline harness/self-test path, not a runtime fallback and not a publication authorization source. There is no Redis, message broker, browser application, or standalone MCP server.

`src/api/server.ts` owns HTTP. `src/worker/index.ts` owns the approval-to-publish lifecycle. `src/scheduler/daily.ts` owns the daily enqueue. `src/harness/orchestrator.ts` is the actual manager: deterministic TypeScript, not the dormant master-prompt manager harness. `src/mcp/*` are imported libraries for image and platform APIs.

### Exclusive worker ownership

**Deployed** — the code is live in the current `44d7336…` release, independently verified 2026-08-28. The production-validated behavioral evidence remains **operator-reported 2026-08-27, not independently re-examined**: the operator reports an approximately 58-second ownership wait before readiness on the first protected deploy, which is this design's predicted behaviour under Render's zero-downtime overlap.

Exactly one worker process may execute work, and that exclusivity is established rather than assumed. Render background-worker deploys are zero-downtime: the new instance starts and only about sixty seconds later does the old instance receive SIGTERM, after which it still gets its shutdown grace. Old and new overlap legitimately, so "my process just started" never implies "the running brief is abandoned".

Ownership is a PostgreSQL **session-level advisory lock** on key `(1889446263, 889784911)`, derived from the namespace `gcd-social:worker-ownership:v1`, held on a **dedicated `pg.Client`** for the process lifetime — never through the shared pool, whose connections are returned, reaped, and cannot issue a matching unlock. PostgreSQL releases the lock the instant the owning session ends, so clean exit, SIGKILL, OOM, and host loss are identical and require no TTL. There is deliberately no reconnect-and-reacquire path: by the time a lost session could reconnect, a successor may legitimately hold the lock.

The invariants, each proven by `npm run test:ownership` against injected boundaries:

- **A non-owner does nothing.** A worker that has not acquired the lock reconciles nothing, emits no readiness, and consumes no work. It waits.
- **Recovery runs only after ownership is acquired**, because recovery is destructive. Ownership-loss monitoring starts before recovery, so ownership lost mid-recovery is caught rather than written through.
- **Recovery terminalizes; it never resumes.** Every remaining `running` brief provably has no live owner and is classified from its durable phase markers, then written to a terminal state. Nothing is resumed, retried, or returned to `pending`, and recovery imports no provider code and issues no provider request.
- **The claim itself is fenced.** The `pending → running` transition executes on the ownership session, so it can only commit while this process is still the exclusive owner, and ownership is rechecked after the commit. A dead session cannot commit, which is what makes the invariant hold by construction rather than by timing. The pool-based `claimNextBrief` remains only for the offline/in-memory path and for callers that are not the owning worker.
- **Durable phase markers are safety state, not telemetry.** `brief:approval_requested`, `brief:publish_attempt_started`, `brief:publish_attempt_settled`, and `brief:publish_attempt_abandoned` commit *before* the side effect they describe, through a distinct failure-propagating primitive. `recordEvent` keeps its best-effort telemetry contract unchanged; these must never be converted back to it.
- **Ownership loss fences future side effects and ends the process.** Approval creation, credential acquisition, and every provider attempt — including between platforms — are blocked. A second barrier is taken immediately before each provider request, so ownership lost after the started marker commits still prevents contact. The process exits nonzero for restart: recurring timers and pooled connections would otherwise keep the event loop alive as a healthy-looking worker that never consumes again.
- **Former owners decline terminal writes**, so they cannot overwrite a successor's recovery. Nothing is lost, because settled markers carry every provider post ID and a successor reconstructs the same outcome.

**Readiness now proves four things at once:** durable state initialized, exclusive ownership held, work abandoned by a previous owner reconciled, and mandatory initialization complete. It must never be emitted before ownership and reconciliation succeed.

Ownership is **mutual exclusion, not a fencing token**: if an owner's connection dies while its process lives, PostgreSQL releases the lock and a successor may reconcile concurrently. The Phase 0A publication guard is the actual fence — the successor's revocation makes the old process's next pre-request authorization check fail closed — which narrows exposure to a single already-in-flight HTTP request, itself classified as an uncertain outcome.

**What this does not solve.** Interruption during a provider attempt still leaves an outcome the system cannot resolve by itself. It is surfaced and nothing retries automatically, but provider-level `withRetry` remains an independent path that can reissue a request after an ambiguous network outcome. There is no durable provider operation ledger, no idempotency key, and no provider reconciliation, so duplicate publication remains possible and a human must reconcile against the platform. That work is the first item under Next hardening in [Roadmap](ROADMAP.md).

### Reasoning-stage execution (Phase 0B.1–0B.6) — all six merged, dormant, not established as deployed

`src/harness/agents/stageExecution.ts` is the reusable boundary through which a registered stage may reach a model. Six stages are now built on it, and **all six are merged**: `strategyConcept.ts` through PR #42 (merge `8c8bd5b0fd500f9a28247f472fd6626bb05c6ebd`); `automotiveTruth.ts` through PR #44 (merge `52050b4d20d03b5cbaf2a98eaab71b2f77685d80`); `hookStoryScript.ts` through PR #46 (merge `c129bbf5a1d35e123aa49c1c5349143bb60ae800`); `productionDirection.ts` through PR #48 (merge `5d3b2cafdfe11b5efc94fbc7fafd387d9a1a67f7`); `packagingAdaptation.ts` through PR #50 (merge `360f89a71f3af85155965e65d2f40d6f705bd795`); and `finalCritic.ts` through PR #52 (merge `ec2a0b78e84386d5491db96418a53c64ee3a8e08`). None is enabled, established as deployed, or production-validated; production evidence for Phase 0B.5 and 0B.6 is none. None of the six is reachable: the worker, scheduler, orchestrator, API, preview, approval, publication, provider, image, Slack, database, and evidence-write paths do not call any executor. Every stage still reports `executionEnabled: false`.

Stage 4 (`production-direction`) narrows the authority again: **stage 3's *used* claims are the boundary — not stage 2's whitelist and not the evidence pack** — and stage 2's output is supplied only to revalidate stage 3's bindings, never shown to the model *(except, since the writer-restriction change below — `IMPLEMENTED`, not merged — its `REQUIRED_CAVEATS` and `FORBIDDEN_CLAIMS`, as binding restrictions)*. Its direction is branded provisional, unverified, non-publishable **and non-executable**, and every production requirement is branded `availabilityVerified: false`. Deterministic validation there cannot prove a shot represents reality, that a requested asset exists or may lawfully be used, that an action is safe, that overlay wording restates its record faithfully, or that every visual implication was cited.

Stage 2 (`automotive-truth`) receives the complete typed Stage 1 output — angle, concept, rationale, hypotheses, assumptions, and all citation arrays — inside one bounded untrusted-data block. It decides what content may assert by building a structural whitelist: a permission is a **binding to an evidence id**, never a sentence. The shared evidence projection exposes each record's authoritative `kind` but not confidence, provenance, reviewer identity, or internal timestamps; the recorded class overrides the model's declaration, and what may be claimed is read back from the records rather than from either stage's prose. It does **not** semantically prove model prose or a restatement true — a language model is not a semantic prover of factual truth here.

Stage 3 (`hook-story-script`) writes the copy inside stage 2's whitelist: the permitted-claim projection is derived from that whitelist alone, so a fact's presence in the evidence pack is not permission to say it, and the complete pack is never offered as an alternate claim source. It revalidates both prior-stage outputs against the same evidence pack rather than trusting their branding. That is **structural validation, not provenance or authenticity verification** — a structurally valid deserialized or hand-built value that binds cleanly to the pack passes, by design. Deterministic validation cannot prove a paraphrase faithful to its cited fact, nor detect every uncited factual implication.

Stage 5 (`packaging-adaptation`) keeps stage 3's actually used claims as its complete factual authority; stage 4 is creative context, not a claim source. Its `google_business_profile` review vocabulary remains distinct from provider `gbp`, bound through exhaustive forward and inverse maps and both runtime round trips. The shared production hashtag tokenizer refuses tag tokens in caption prose, canonical tags live only in the dedicated array, and proposed caption/separator/tag text must fit the imported provider-visible limit **less a per-platform contact-line reserve** (`CONTACT_LINE_RESERVE_CHARS`: Instagram 64, Facebook 192, Google Business Profile 0), so the caption budget is 2,136 / 2,008 / 1,500. Dedicated URL/destination fields are structurally absent, while one syntax guard rejects explicit URI schemes and `www.` tokens from caption, local keywords, open questions, and claim-use summaries. That guard does not claim semantic understanding of obfuscated or indirect destinations.

**Stage 2's restrictions bind the writing stages (`IMPLEMENTED` on branch `claude/great-archimedes-2oysf0`, not merged, not deployed).** Stage 2's `requiredCaveats` and `forbiddenClaims` become **binding restrictions** on stages 3, 4 and 5. Stage 3 already received them inside `TRUTH_OUTPUT`; its prompt no longer calls them advisory. Stages 4 and 5 now receive two more untrusted blocks after their own, `REQUIRED_CAVEATS` and `FORBIDDEN_CLAIMS`, rendered by `renderRequiredCaveats`/`renderForbiddenClaims` — which moved to `automotiveTruth.ts` so the writers and the critic's evidence lens use one function each and receive byte-identical blocks (`finalCritic.ts` re-exports them) — at the evidence lens's own ceilings (`WRITER_RESTRICTION_BLOCKS` in `payloadContract.ts`); each writer refuses a block over its ceiling before any request. The lists can only narrow what a writer may say, never permit anything: each writer's claim block (`PERMITTED_CLAIMS` or `SCRIPT_CLAIMS`) stays its only source of assertable fact, and a caveat that seems to need an unbound fact is omitted and raised as an open question. Stage 2's `assessment`, restatements and wider whitelist stay withheld from stages 4 and 5; stage 3 still receives them inside `TRUTH_OUTPUT`, unchanged. All three writers also load `skills/claim-boundaries/SKILL.md`, which gains a fact-free attribution rule. No code checks copy against either list; the evidence lens checks the finished copy against both. The data flow changes inside dormant stage code only: no stage is enabled or reachable.

**The deterministic contact line (`MERGED` through PR #88, not deployed).** After stage 5 validates and before the critic, `src/harness/agents/contactLine.ts` attaches a typed `contact` object — `{ kind: "deterministic_contact", text, gbpCta?, sourceFactIds }` — to every package. **No model writes or edits any part of it.** The shop name, phone number and booking link are copied byte for byte from the evidence pack's `approved-facts:shop`, `approved-facts:phone` and `approved-facts:bookingurl` records (the substring after the record's `"<field>: "` prefix), using owner-reviewed templates verbatim: Instagram `Call {shop}: {phone}`; Facebook `Call {shop}: {phone} · Book online: {bookingUrl}` — with today's facts, `Call German Car Depot: …`, exactly as the owner reviewed them. (PR #88 typed the shop name into the module; reading it from the `shop` record, failing closed like the phone number, is part of the narrow critic panel change — `MERGED` through PR #89.) Google Business Profile no text and a structured `{ actionType: "BOOK", url: {bookingUrl} }` call to action, mirroring the BOOK rule `ctaForGbp` in `packageMap.ts` applies to the live path (read, not changed). `sourceFactIds` lists exactly the records used. A needed record that is missing, not a usable verified business fact, or a line longer than its reserve (in characters and in UTF-8 bytes) is a `ContactLineError` before any paid call. Contact text is not caption prose: it sits outside the caption, so stage 5's URL ban keeps applying to model prose only. Caption + separator + hashtags + separator + contact text stays within each platform's existing limit. Stages 3 and 5 are told not to name a contact or booking channel, because code adds the line. The contact step reaches no deployed path and builds no provider payload; the contacted package is still provisional, unverified, non-publishable and non-executable.

Stage 6 (`final-critic`, **merged** through PR #52) reviews stage 5's finished package against a further-narrowed authority: not stage 3's whole used-claim set, but stage 5's own typed, per-platform claim bindings (`PLATFORM_CLAIMS`), read through stage 5's own evidence accessor. It revalidates the entire prior chain — stages 2 through 5 — via each owning stage's exported revalidator, including a new `revalidatePackagingAdaptationOutput(...)` in `packagingAdaptation.ts`, and separately checks that the requested-platform sequence exactly matches stage 5's own package sequence. The packaging it reviews is stage 5's output with the contact line attached: `revalidateContactedPackagingOutput(...)` requires a `contact` on every package, revalidates the stage 5 part through stage 5's own revalidator, rebuilds every contact line from the pack and refuses any that differs — before any model call.

**Stage 6 is a narrow critic panel (`MERGED` through PR #89, merge `e557dda…`; not deployed).** `final-critic` stays one registered stage, order 6, but its executor reviews through four lenses, each **exactly one model request** through the shared boundary on the `critic` policy — four requests, concurrently, no retries: **evidence-fidelity** (`claim_fidelity`, `uncited_implication`; shown stage 3's hook, beats and script, stage 4's overlay text with each overlay's shot subject and — since the follow-up, `MERGED` through PR #90, not deployed — `shotFactIds`, the ids of the records stage 4's `claimVisuals` bound to that overlay's shot (read from the typed binding, looked up in `SCRIPT_CLAIMS`, never the binding's direction-summary prose), each package's caption, hashtags, local keywords and contact line, `SCRIPT_CLAIMS`, `PLATFORM_CLAIMS`, and — reviewer-only — stage 2's `REQUIRED_CAVEATS` and `FORBIDDEN_CLAIMS`, which stages 4 and 5 also receive since the writer-restriction change above, `IMPLEMENTED` and not merged); **platform-and-local** (`platform_semantics`, `hashtag_keyword_relevance`, `timing`; shown the contacted `PACKAGING_OUTPUT`, `REQUESTED_PLATFORMS` and `PLATFORM_CLAIMS`); **voice-and-craft** (`voice_clarity`; shown only the hook, the script and each caption); **production-coherence** (`production_coherence`; shown `SCRIPT_OUTPUT`, `PRODUCTION_OUTPUT` and the contacted `PACKAGING_OUTPUT`). Any lens may raise `human_decision`; the validator refuses any other category from a lens. Each lens has its own tool-free prompt (`agents/final-critic-{evidence,platform,voice,production}.md`), its own fact-free skills (`critique-discipline` for every lens, `claim-boundaries`, the new `platform-local-review` — never `platform-specs` or `local-seo`, which carry facts — `script-craft` and `adaptation-craft`, `production-craft`), its own output contract, and its own token floor; `CRITIC_LENS_BLOCKS` in `payloadContract.ts` fixes what each lens is shown. `invokeStage` loads the stage's declared assets and puts only the requesting lens's prompt and skills into its instruction channel (the rest are recorded `omitted`); a stage declaring more than one prompt must name them per request, and an undeclared asset is refused. Stage 2's caveats and forbidden claims reach the evidence lens only among the lenses: a reviewer writes no copy, so seeing them gives it nothing to put into a caption; stage 2's assessment and restatements reach no lens. *(With the writer-restriction change above, stages 4 and 5 receive the same two blocks as binding restrictions.)* **If any lens fails** — a request error, a refused or truncated response, non-strict JSON, a validation failure — the stage fails closed with a `CriticPanelError` naming every failed lens, after every lens has settled; no partial critique is returned. **No model writes or merges the combined output**: TypeScript takes the union of every lens's findings in lens order, each carrying its `lens`, with no deduplication; computes the verdict, owner-aware since the follow-up (`MERGED` through PR #90, not deployed): a blocking finding owned by a revisable stage → `needs_revision`; else a blocking finding owned by `human_review`, any `human_decision` finding or any lens verdict `needs_human_review` → `needs_human_review`; else `provisional_pass` (PR #89 merged the earlier rule, under which any blocking finding gave `needs_revision`); writes a deterministic summary of finding counts per lens and severity; and keeps each lens's own model-written summary, attributed to it, in `lenses`. Every assessment — the panel's and each lens's — is branded with five literal-`false` fields — `authoritative`, `approvalGranted`, `publishable`, `executable`, `productionValidated` — with no field in any lens contract through which a model could set one true; a structural (not correctness) check relates each lens's own `verdict` to its findings' `severity` and `owner` (a revisable Stage 3/4/5 stage, or `human_review`). It does not replace the existing, currently-running `brand-compliance-critic` gate, which is unchanged.

**Payload boundary across all six stages — reconciled and `MERGED`.** The mismatch this paragraph used to record is closed in code and present on `main` through PR #54 (merge `0c13ab1af9c7ca796a1d48ed37207715a47166e4`), which is **not established as deployed, not enabled, and not production-validated**. Merge changed repository state only; it authorizes no production wiring. `src/harness/agents/payloadContract.ts` is now the single authority for every bound the six stages apply: evidence limits, independent record/conflict projection cardinalities, each stage's field limits and serialized ceiling, every producer/consumer guard, every assembled-payload ceiling, the shared `MAX_PAYLOAD_CHARS`, and per-policy output-token floors. It imports nothing. Each ceiling is derived from a maximum-cardinality shape witness plus bounded string content at `MAX_JSON_ESCAPE_EXPANSION` (**2**); every bounded output string also applies the same numeric UTF-8 byte cap. Every consumer guard exactly equals its producer ceiling (Stage 1 66,022; Stage 2 73,859; Stage 3 72,621; Stage 4 87,531; Stage 5 98,084 — and, for the contacted handoff stage 6 actually receives, `CONTACTED_PACKAGING_OUTPUT` 104,852 (103,604 at PR #88's merge, before the contact line cited the shop-name record; 98,084 before the contact line), and no token budget moves with it; the `final-critic` assembled ceiling is its largest lens request, production-coherence at 265,277 — 327,021 as a single critic), and `MAX_PAYLOAD_CHARS` remains the largest assembled stage ceiling rounded to the next 10,000 — **410,000**. These moved from 33,022 / 39,459 / 40,621 / 68,331 / 78,884 and 380,000 when every internal-plumbing character field was given a hidden margin (see the output-field classification below); before that, Stage 1's guard and the shared boundary had moved from 32,422 and 370,000 when `STRATEGY_LIMITS.conceptChars` rose from 1,200 to 1,500. The pack builder actually enforces 64 projected records, and the owning evidence-record validator runs again at pack construction, durable-row reconstruction, pack projection, and the shared pre-model boundary. Exhaustive conflicts have a separate enforced maximum of 64 entries: 64 same-subject/same-attribute facts produce all 2,016 pairs, and such a pack is refused intact by the renderer and shared pre-model boundary rather than silently losing any disagreement. Evidence text is checked in characters/code units and UTF-8 bytes. `detail` uses a 4,000-byte PostgreSQL-canonical `jsonb::text` contract, computed conservatively in TypeScript including the 327-byte signed `-5e-324` representation; relation notes use their 500 bound at TypeScript relation boundaries. Migration 007 mirrors the database contract, rejects NULL tag elements, and creates a uniquely versioned helper with plain `CREATE`, so an unexpected collision fails without overwriting unrelated code. Its 208-check apply/enforce/rollback/reapply and collision-refusal paths pass on disposable PostgreSQL 16.15 and 18.6. **Migration 007 is now production `APPLIED`**: M1 (an API-only deployment of exact artifact `A` = `d5015236672a02bf8f58d342625c32a4f5acc8a1`) was independently verified 2026-09-18, with `_migrations` recording it applied inside that deploy window — see [Status](STATUS.md). The token floors are derived from the escaping-aware serialized UTF-8 ceilings at one token per byte (87,000 / 99,000 / 111,000 — the critic's is its largest lens floor, 111,000 for evidence-fidelity and platform-and-local and 46,000 for the other two; it was 110,000 as a single critic and 102,000 before its `issue` margin — each below the 128,000-token model output cap and asserted so, and every lens floor leaving `THINKING_RESERVE_TOKENS` under the critic's 128,000 `max_tokens`), not from an ordinary-prose estimate. Anthropic adaptive thinking shares `max_tokens`, so the centralized typed stage policy explicitly disables thinking for `reasoning-heavy` and `reasoning-standard` and makes the full derived ceiling available to visible JSON; the `critic` policy thinks — see the next section — and its budget is the model's whole cap. Model selection, thinking, effort and the 128,000-token caps remain centralized in `modelPolicy.ts`. PR #54 left legacy agent and vision behavior unchanged; the legacy path's own model ids and thinking configuration were set separately — see the orchestrator agent-loading section below. Stage 5's 2,200-character caption narrowing and Stage 6's ids-only `PLATFORM_CLAIMS` projection remain unchanged; the contact-line reserve lowers the caption budget beneath that narrowing (Instagram 2,136, Facebook 2,008; Google Business Profile unchanged at 1,500). A pack is also checked for **meaning**, not only bounds: `assertUsableEvidencePack` is the one runtime validator every pack consumer calls, enforcing section-permitted kinds, lifecycle in both directions, one section per record, `allowedFacts` restricted to active fresh verified facts, conflicted/stale/inactive material barred from remaining usable, every conflict field and the closed `basis` enum, honest `counts`, the pack's `goal`, and a round-tripping ISO-8601 `builtAt` — which is also the anchor freshness is evaluated at, a decision documented with the gap it leaves. A conflicted record is held in a dedicated, non-citable `conflictedEvidence` section rather than removed from the pack, so a conflict's two ids always resolve to real records and its `aClaim`/`bClaim`/`subject` are checked as exact snapshots of them. Stage calls make exactly one **provider** request (`maxRetries: 0` at the SDK boundary, against an SDK default of 2) and stream, because the declared `max_tokens` cannot be received otherwise. Two separate bounds apply: the SDK's `timeout` covers request setup only — it is cleared when streaming headers arrive — so the stage boundary arms its own budget-derived deadline around the whole stream and aborts it if that expires. **These bounds are derived and regression-tested, not production-validated** — every stage still reports `executionEnabled: false` and oversized input fails closed before any model call. Production evidence remains only the operator's 2026-09-02 read-only aggregate audit finding both evidence tables empty. See [Roadmap](ROADMAP.md).

The boundary's guarantees, and the reason each exists:

| Guarantee | Why |
|---|---|
| Exactly one **provider** request per stage — for `final-critic` only, exactly one per lens, four lenses — with no retry and no repair call | A silent retry turns one budgeted decision into unbounded spend; a repair pass is a second chance to argue into an unsupported claim. The Anthropic SDK defaults `maxRetries` to 2, so this holds only because the stage boundary sets it to **0** explicitly — until it did, "one request" described one wrapper invocation and up to three wire attempts. The critic panel's four lens requests are four invocations of the same boundary, each one request, none retried |
| No tools registered; capabilities closed to what the registry declared | Registration is not permission — a widened capability must be a reviewed code change, not a registry edit |
| Instructions come only from registry assets | There is no way to pass free-form system text through the boundary, so the checked-in prompt is always the contract |
| Untrusted inputs framed as labelled data blocks | A stage that concatenates a goal into its instructions is one brief away from executing whatever the author typed |
| Fail closed on missing credentials, assets, timeout, runner error, or invalid output | Nothing degrades to a partial or best-effort result |
| No logging of prompts, evidence, model text, or secrets | The module writes no log lines at all |

Model selection resolves in `modelPolicy.ts` alone: the registry declares a class (`reasoning-heavy`), the executor names no id, and `deterministic-only` refuses to resolve rather than acquiring a model by default.

**A configuration the provider would reject cannot resolve.** Disabling thinking is not universally free: on `claude-opus-5` — what `reasoning-heavy` resolves to — `thinking: {type: "disabled"}` is accepted **only at effort `high` or below**, and pairing it with `xhigh` or `max` returns a **400**. `modelPolicy.ts` therefore declares the ordered `EFFORT_LEVELS`, the limit `MAX_EFFORT_WITH_THINKING_DISABLED`, a per-policy `POLICY_EFFORT` table, and the set `MODELS_REJECTING_DISABLED_THINKING_ABOVE_HIGH` — which holds `claude-opus-5` and nothing else, because the restriction is a property of the **model id**, not of a policy name: repointing a policy carries the restriction with the id. `claude-sonnet-5` is deliberately absent, no such restriction being documented for it, and adding a model requires a documented provider source. `resolveModelPolicy()` enforces one invariant over all three facts — thinking disabled, model restricted, declared effort above the limit — which catches both the edit that raises an effort and the edit that disables thinking under an existing one. It throws at resolve time, which is **before** any request is built and before anything is billed, so a knowable 400 costs nothing instead of one discarded paid stage call. **The critic is the one policy that thinks.** It resolves to `claude-opus-5-5`, which rejects `thinking: {type: "disabled"}` at **every** effort level, so `POLICY_THINKING.critic` is `{type: "adaptive"}` and `MODELS_REQUIRING_THINKING` records that id with its quoted source; the same invariant, written once as `thinkingEffortViolation()`, now also throws when thinking is disabled on a model in that set. `POLICY_EFFORT` declares one effort, `critic: "high"`, set explicitly because Opus 5.5 defaults to `medium`; `reasoning-heavy` and `reasoning-standard` declare none and their requests carry no `output_config.effort`. The declared effort now reaches the wire — `resolveModelPolicy` → `invokeStage` → `StageRunnerRequest.effort` → `runStageAgent` → `output_config.effort` — through a stage-only `StageRunOptions`, so no legacy request can carry one, and `sdk.ts` re-checks the invariant where the stage request is built. Because thinking tokens count against `max_tokens`, the critic's budget is its model's whole 128,000 cap; a named, heuristic `THINKING_RESERVE_TOKENS` must remain between its contract floor and that cap, and `ResolvedModelPolicy` carries `visibleOutputTokens` only when thinking is disabled. None of this is deployed: no deployed code path resolves the critic policy.

**A stage response must finish its turn.** `runStageAgentWithStreamOpener` checks `stop_reason` before `collect()` reads any content: `max_tokens` raises `StageOutputTruncatedError`, `refusal` raises `StageRefusalError` (with `stop_details` guarded, since it may be `null`), and anything but `end_turn` raises `StageUnexpectedStopError`. Each carries the complete provider message so the local CLI can save it. No `fallbacks` parameter or refusal middleware exists: a fallback would answer silently with a different model. `collect()` is shared with the deployed legacy path and is unchanged.

Validation is where the epistemic contract is actually enforced. The model's cited ids are bound to the exact evidence section the contract assigns them, so a performance or hypothesis id cannot be returned as a verified fact, and conflicted, stale, or inactive ids are rejected even when the id is real.

**The stage contract has two halves, and both are checked.** The validator half is `payloadContract.ts` plus each stage's own validator. The prompt half is the checked-in Markdown in `agents/`, which is the only instruction channel the boundary has: every stage prompt states the output limits its own validator enforces — each bounded string's character ceiling and each array's cardinality, and for `packaging-adaptation` the per-platform caption, hashtag and local-keyword ceiling (Google Business Profile: at most two local keywords) at the **effective** number (`Math.min` of the provider policy and this pipeline's narrowing, less the contact-line reserve — so Facebook's stated 2,008 rather than its 63,206 provider limit). Stage 5's response schema states the same per-platform caption budgets in its `caption` description. This is load-bearing rather than courteous, because the boundary makes exactly one provider request per invocation (one per stage; one per lens for `final-critic`'s four lens prompts, each of which states its own lens's ceilings) with no retry and no repair pass: a ceiling the prompt omits is discovered only by a completed, paid-for response being discarded whole. The `CD0`–`CD8` group in the offline suite asserts the two halves agree, keyed off the validators' own limit objects, so changing a limit fails the suite instead of silently desynchronising the prompt. The prompts state each ceiling as a ceiling and not a target — cite what genuinely supports the work, and never more than N — and the pre-existing "an empty array is honest" guidance is preserved and separately asserted.

**Output fields are classified, and — with one reviewer-only exception — only one kind gets a margin.** The skills govern craft; the payload contract governs size; and where the skills deliberately say nothing, the budget decides. `OUTPUT_FIELD_BOUNDS` in `payloadContract.ts` classifies every bounded output field, beside its limit, as **product-bearing** — its content reaches a human reviewer (rendered by the run summary, or named by the prompt as a human's to read), a platform payload, or a filming instruction — or as **internal plumbing**: a stage explaining itself, a handoff to the next, or the model's own gloss beside an evidence-id binding. A product-bearing limit is a product decision, sometimes deliberately stricter than the platform (`skills/platform-specs` specifies 8–15 Instagram hashtags where the platform permits 30), and it states exactly what it enforces — except a field whose sole reader is the internal human reviewer and which no platform or provider receives: `REVIEWER_ONLY_MARGIN_FIELDS` lists exactly one field, the critic's `findings[].issue` (stated 400, enforced 600) — once per critic lens since the panel, as `final-critic:<lens>.findings[].issue`. A plumbing limit has no research behind it — `skills/script-craft` is "craft only" and excludes "character-count trimming" — and exists only to keep the payload and token budget finite, so each plumbing character field keeps its prompt-stated figure (the model's aim point, measured to be where it lands: `concept` 1,259 against 1,200, `rationale` 2,580 against 2,000) while its validator enforces a wider ceiling the model is never told about, through the prompt or the schema `description`. The margins are sized from each policy's output-budget headroom rather than by one multiplier — 3× for stages 1–4, 2× for stage 5, 2.5× for stage 6, never below `CEILING_SLACK_MULTIPLIER` (2) — and every derived budget is asserted strictly below its model's output cap. The offline suite fails if a plumbing character field lacks a margin, a product-bearing field outside that allow-list is given one, the allow-list changes, or a plumbing field appears on the review surface. The local evaluation CLI measures every bounded field of every provider response against these limits on every run, so the next overshoot is data rather than a surprise.

**The provider is constrained to the response shape, not asked for it.** Each stage sends a JSON
Schema as `output_config.format`, built beside the validator that enforces it from the same
`ALLOWED_OUTPUT_FIELDS` array `requireExactKeys` reads and the same exported enums, so the shape
generation is constrained to and the shape validation demands cannot drift. This replaces an
instruction with a constraint: every stage prompt already said "no markdown fence", and a paid
stage-3 response arrived fenced anyway. The limits of the mechanism are part of the design —
structured outputs enforce `type`, `properties`, `required`, `additionalProperties`, `enum`,
`const` and string formats, and do **not** enforce `maxLength`, `maxItems`, `minimum` or `pattern`
— so every character and cardinality ceiling remains exactly where it was, in the prompt and in the
validator, and the offline suite fails any schema that claims otherwise.

**Nothing buys a stage before a free check can fail.** Validation runs *after* the provider
returns and one rejection ends the run, so any precondition knowable beforehand must be checked
beforehand or it is paid for. The operator-local evaluation CLI, `scripts/local/content-run.mjs`
— the only path that executes these otherwise dormant stages, invoked by hand and never by a
service — enforces three such preconditions ahead of its cost gate: a `--runner live` run refuses
an absent automotive-facts file rather than warning past it; every stage's `requiredEvidenceKinds`
is checked against the built pack across all of `TARGET_STAGE_IDS`, because `stageExecution.ts`
checks that per stage as each one runs and so discovers a class only stage 2 needs after stage 1
has been billed; and any response the provider already returned is written to
`rejected-responses.json` beside the run rather than discarded with the error, with a
`field-measurements.md` table of every bounded field's size against its limit written beside it on
every run, passing or failing. None of this
weakens the boundary: `stageExecution.ts` keeps its per-stage check, its single request, and its
no-retry, no-repair guarantee, and nothing captured is read back as stage output. Size rejections
also report the measured length alongside the bound, since a fifty-character overrun and a
two-thousand-character overrun call for different fixes. Every run also writes `run-meta.json` —
the goal, the run's instant, and sha256 fingerprints of `config/approved-facts.json`, of the
automotive facts file and of the evidence-pack projection — and a live run requires the word `LIVE`
typed at the prompt after the cost flag. `--replay-critic <run-dir>` runs only `final-critic` —
all four lens requests —
against a saved run's stage outputs, into a new sibling directory, refusing unless those
fingerprints match and every saved output revalidates through its owning stage's validator. Both
modes check every record the deterministic contact line needs before the cost gate, attach the
contact lines after stage 5 (a saved stage 5 output that no longer fits under the contact-line
reserve is refused on replay, naming the reserve), write them to `05b-contact-lines.json`, and
hand the critic the contacted packages. `summary.md` renders each platform's contact line and
Google Business Profile's call to action beside its caption, then the critic panel — its computed
verdict and counts, and one section per lens with that lens's verdict, its own summary and its own
findings — and its footer names the runner that actually ran (`_Fake-runner output…_` or
`_Live-runner output…_`, each followed by "Not reviewed. Not publishable. Authorizes nothing.").
Field measurements key each critic lens response by `final-critic:<lens>`, against that lens's own
limits, and the printed cost ceiling counts one request per lens: nine requests for a full run, four
for a critic-only replay (`MERGED` through PR #89).

### Content Intelligence foundation (Phase 0B.0) — merged and deployed

Additive and inert. It changes no production behavior: the scheduled pipeline below is untouched, and no reasoning stage executes.

**Content evidence.** `src/harness/evidence/` defines a typed contract in which epistemic class is a property of every record rather than a convention. Eight kinds — verified automotive fact, verified business fact, sourced research, GCD direct observation, GCD performance evidence, creative hypothesis, causal hypothesis, unsupported assumption — with per-kind validation, mirrored as CHECK constraints in migration 006 so the invariant survives any writer that bypasses the application layer. Two promotions are impossible by construction: a model-authored hypothesis can never become a verified fact, and a performance measurement can never become automotive or causal truth.

**Evidence pack.** `buildEvidencePack()` groups claims by class and pulls out conflicts and stale material. It **reports conflicts and never resolves them** — choosing the newer or more confident row would manufacture certainty from genuine disagreement — and stale fact-class evidence fails closed out of `allowedFacts`. A conflict is two different claims about the same subject *attribute*; distinct attributes of one subject do not disagree.

**Approved facts stay authoritative.** The adapter is a deterministic projection of `config/approved-facts.json`, carrying provenance and the exact content hash. It creates no second source of truth, and import is an explicit operator command rather than a startup side effect. The live copywriter and critic continue to cite the JSON directly until a later reviewed cutover.

**AgentRegistry.** `src/harness/agents/registry.ts` registers the six target stages with model policy, prompt/skill/reference assets, allowed capabilities, required evidence kinds, input/output validators, and prerequisites. Asset paths are repository-relative and allowlist-rooted; traversal is rejected at registration, and a missing mandatory asset fails loudly rather than silently running a stage without its instructions. `executionEnabled` is `false` on every stage.

**Preview.** `POST /console/content-intelligence/preview`, behind the existing console credential, builds the pack, resolves the stage plan, verifies every asset, and returns structured JSON. It calls no model, image provider, or social platform, creates no approval, and enqueues no brief; `assertPreviewIsInert()` makes that a checked property rather than a promise.

## Delivery control plane

Pull requests and `main` pushes enter `.github/workflows/ci.yml`. A production release can begin only from a successful, push-originated workflow named `CI` on this exact repository's `main`, only after the exact-string repository variable gate is enabled, and only inside the serialized GitHub `production` environment. The triggering run supplies `TARGET_SHA`; no dispatch input or artifact does. After the release concurrency slot has been acquired and `origin/main` refreshed, `scripts/render/deployment-controller.mjs` requires `TARGET_SHA` to equal current main before any Render command. A stale result is reported as superseded with no deployment. The controller then derives the actual API `LIVE_SHA`, verifies both commits in fetched repository history, performs no deployment when all three services already report the target, and checks `LIVE_SHA..TARGET_SHA` for `state/migrations/**` before any deploy. Migration changes stop for a separately controlled rollout. Otherwise it performs API → exact public health → worker → target-bound readiness and stabilization → scheduler → all-service SHA verification, one service at a time. Health must come without redirect from the exact checked-in GCD URL and report the expected service, PostgreSQL state, and `TARGET_SHA`. A present Content-Length is an early rejection hint only; the controller independently counts a nonempty BYOB byte stream into a 4,097-byte buffer, cancels on the one-byte overflow probe, uses fatal UTF-8 decoding, and carries the same 10-second abort through fetch and body read. The worker's single structured ready event is emitted immediately before queue consumption, and the controller then observes bounded logs for 10 seconds before permitting the scheduler. In current production that event follows durable state initialization, exclusive ownership acquisition, and interrupted-work reconciliation, which is why the readiness window is 60 attempts at five-second intervals rather than the original ~55 seconds: readiness cannot appear until the previous instance's session ends. The window was widened, never weakened — exact `TARGET_SHA` equality, instance correlation, malformed-marker rejection, stabilization, crash/restart detection, and fail-closed behaviour at the bound are unchanged, and a previous instance's marker is emitted at `LIVE_SHA` and can never satisfy the target check.

**Independently verified 2026-08-28** by a separate final-inspection session: Render native auto-deploy was off on all three services and the GitHub controller was configured with its repository gate `false`, leaving production in an intentional zero-unattended-authority window. Render CLI is the GitHub machine interface; Render MCP is a read-oriented interactive diagnostic interface unless an operator separately authorizes a write. See [Deployment control](DEPLOYMENT.md).

## Orchestration lifecycle

1. A scheduled or manual brief is persisted in `brief_queue`.
2. A worker atomically claims the oldest pending row and marks it `running` (`FOR UPDATE SKIP LOCKED`). In current production that claim executes on the worker's ownership session, so it can only commit while that process is still the exclusive owner.
3. Code replaces only caller-supplied `approvedFacts` with `config/approved-facts.json`, preserving scheduler-authored context fields such as theme, make, service, and day index. Analytics runs best-effort, then copywriter/image-specification/SEO model calls fan out concurrently against that trusted brief.
4. Code ignores model-authored media URL, QC, and provenance fields. It normalizes the requested shared-feed size to one of `1080x1350`, `1080x1080`, `1200x900`, or `1200x630` (default `1080x1350`), then asks the provider for a **provider-friendly source render at that same aspect ratio** — 1024x1280 for the 4:5 profile — and accepts only a direct HTTPS `fal.media` URL without credentials, fragment, nonstandard port, or redirect. The intermediate download is bounded to 20 MiB and 30 seconds.

   **The provider render is an input, not the artifact.** Two policies apply, and conflating them is what blocked scheduled posting in August 2026: *decode safety* governs bytes we are willing to process (supported format, ≤4,096 px per side, ≤16 million px), while the *publication profile* governs bytes we are willing to publish. A provider-native 896x1120 render, or a PNG returned despite a JPEG request, is a perfectly valid input; neither is publishable until this pipeline produces the exact profile. Providers guarantee composition, not exact publication pixels.

   Normalization is therefore: decode-safety validation → uniform-scale plan → resize → JPEG transcode at deterministic quality 90 → reject output over 5 MiB → assert the exact approved profile. **The only sanctioned transformation is a pure uniform scale.** Cropping and padding are refused rather than unimplemented: cropping a 1:1 render into 4:5 would discard 20% of the frame straight through the headline of a text-bearing brand graphic, and padding would alter the composition under review. Uniformity is enforced literally, by **exact rational aspect equality** (integer cross-multiplication), not a near-ratio tolerance: only exact equality guarantees the horizontal and vertical scale factors are the same number, so a source whose ratio is merely close — 897x1121 against a 4:5 target — fails closed rather than being stretched. Any aspect mismatch is a deterministic media-contract failure and is never retried, because the request is byte-identical on every attempt. Mandatory vision QC then runs on the normalized bytes. The strict QC contract includes transcribed text, garble, unsafe, and issue fields; unsafe covers identifiable people/features, readable plates/VIN/contact/customer documents, unsafe shop practice, and material misrepresentation. Only passing bytes are stored and exposed through an HTTPS `PUBLIC_BASE_URL` as `/media/<uuid>-<sha256>.jpg`; one artifact is shared without separate platform crops, and initial generation plus every critic-requested image revision use this same fail-closed path.
5. On every cycle, the platform formatter runs first. Code then constructs the entire canonical `gcd-final-package/v1`, including exact provider-bound text, already-applied hashtags/truncation, language layout, supported media URL/digest, GBP CTA, and a non-secret destination (`accountId`, optional GBP `locationId`, `apiHost`, `apiVersion`) selected from runtime configuration. Deterministic validation checks review/provider equivalence and platform rules, then the package/provider array is recursively frozen before the critic evaluates that exact final package. The provider array must be nonempty, every item must pass strict package validation, and each platform may appear only once. Instagram requires exactly one image with alt/AI disclosure and 8–15 unique provider-visible hashtags equal to the canonical list; Facebook permits at most two hashtags; GBP permits none and requires explicit language/topic type. Because current native FB/GBP request builders send only the media URL, their provider payloads omit unsupported alt/AI fields while the package-level image record preserves runtime provenance. Tokens never enter the approval subject.
6. A PASS requires both deterministic validation and critic PASS. Actionable findings route back to copy, image, SEO, or formatter; the full formatter → canonical package → validation → critic sequence repeats, up to three cycles. Failure escalates without approval. Any best-effort Slack escalation renders bounded goal/reason/run-ID fields as inert text, neutralizing control characters, links, mentions, and backticks.
7. Before creating a row, the worker validates the approval delivery configuration. Every environment running worker approval delivery requires an exact HTTPS `hooks.slack.com/services/...` webhook; a blank value is valid only for paths that do not run that flow, such as direct offline `createApproval` tests. Production additionally requires `PUBLIC_BASE_URL` to be a public root HTTPS origin. It revalidates the whole subject, canonicalizes the exact `PostPackage[]`, stores it as a typed approval subject with its SHA-256, stores only the SHA-256 of a random decision token, and sends the token-bearing review link to Slack without following redirects. Each model-authored summary line is bounded, sanitized, and then code-delimited so controls, mrkdwn, links/bare domains, mentions, and backticks remain inert; the generated review URL is explicitly labeled authoritative and is the message's sole active link. Delivery is retried with a 10-second timeout per attempt. Failed or uncertain delivery must end in confirmed revocation; if revocation cannot be confirmed after bounded retries, a composite error exposes both failures for reconciliation and no approval handle is returned. Decision and publication authorization expire after 24 hours by default. At most one pending decision wins the atomic database transition, and decision handling revalidates the entire subject.
8. After approval, the worker reloads the stored provider array; no content transformation follows. The posting library requires the whole subject to remain nonempty, strict-valid, and unique by platform, rechecks current durable approval/hash/decision/type/status/expiry/revocation/index/exact payload at entry, and passes a frozen stored clone plus a module-issued exact-package guard to the native provider; a caller-created no-op guard is rejected. Every admissible provider awaits that guard immediately before every provider HTTP attempt, inside every read, retry, poll, and multi-step flow. The guard repeats the whole-subject and approval checks, revalidates runtime credentials/configuration against the approved account/location/host/version, and verifies every content-addressed media reference against the database's immutable content fields/current byte digest plus the 5-MiB/JPEG/allowed-profile policy; request construction enforces the same target match. Native requests refuse redirects and require a provider post ID for publication success.
9. The worker records final brief/approval status and telemetry. In current production, durable phase markers commit before each side effect throughout steps 7–8 — approval requested, publish attempt started, settled, or abandoned — and every terminal write is declined if this process is no longer the exclusive owner, so it cannot overwrite a successor's recovery.

## Prompt and skill reality

The orchestrator loads each `agents/<name>.md`, strips YAML frontmatter, extracts only `model` (regex `/^model:\s*(.+)\s*$/m`), and sends the Markdown body plus JSON input to Anthropic. Because `src/worker/index.ts` imports `runBrief` from the orchestrator, those frontmatter ids are what the deployed worker sends; the `model: <id>` line shape is therefore load-bearing. Frontmatter `tools` is not enforced. Agent text says to load skills, but the SDK call registers no tools and does not append `skills/*` content. Skills remain authoritative human specifications only to the extent their rules are repeated in the actual agent body or code.

**Legacy-path thinking is resolved explicitly, not inherited.** This path is non-streaming, defaults to `max_tokens: 3000`, and times out at 90 seconds, and `max_tokens` bounds thinking tokens and visible text together while `collect()` reads only `text` blocks. Omitting the `thinking` parameter runs **adaptive thinking** on `claude-sonnet-5` and `claude-opus-5`, and none on `claude-haiku-4-5` and `claude-sonnet-4-6`. `resolveLegacyThinking()` in `sdk.ts` therefore pins `thinking: { type: "disabled" }` for the ids this path actually routes that would otherwise think by omission — currently `claude-sonnet-5` — for both `buildRequest` (text) and `runVision` (image QC), and leaves the request byte-identical for every other id. `claude-opus-5` is deliberately excluded: only the dormant `agentLoop.ts` manager sends it here, and that path's thinking behaviour is owned separately (see `docs/ENVIRONMENT.md`, `MANAGER_MODEL`). An explicit caller value always wins, so Content Intelligence stages — which set their own thinking policy through `modelPolicy.ts` — are unaffected. Without this, a model-id change alone would silently alter request semantics on a live path with a small token ceiling.

`prompts/MASTER_PROMPT.md` is loaded only by `runManagerTurn` in `agentLoop.ts`, which has no package script or worker caller. The production worker does not invoke an Opus manager agent. Autonomy phases B/C and self-improvement are therefore partial scaffolding rather than complete runtime features.

## Target Content Intelligence architecture

The earlier research named roughly 22 conceptual specialists. That is not a requirement for 22 production model calls. Phase 0B should converge on about six primary reasoning stages:

1. strategy-concept;
2. automotive-truth;
3. hook-story-script;
4. production-direction;
5. packaging-adaptation; and
6. final-critic.

Other specialists should generally become deterministic services, policy/rule modules, reference material, or optional invocations. Science and evidence should be represented deterministically where possible. Phase 0B must add an `AgentRegistry`, real skill/reference handling, research/reference retrieval, deterministic validation around every agent output, and structured evidence capture. The registry (0B.0), the first stage executor with typed output validation (0B.1), the merged second executor that consumes that complete typed output and creates evidence-bound claim constraints (0B.2), the merged third executor that writes copy bounded by those constraints (0B.3), the merged fourth executor that directs the visual plan inside the claims stage 3 used (0B.4), the merged fifth executor that adapts the script into per-platform review copy inside the same used-claim set (0B.5), and the merged sixth executor that adversarially critiques the finished package inside stage 5's own per-platform bindings (0B.6, PR #52) now all exist. Stage 5 retains its review-only `google_business_profile` vocabulary while exhaustive bidirectional maps bind it to provider `gbp`; captions cannot hide hashtag tokens, provider-visible caption-plus-tag length uses the production policy, and recognizable URL syntax is refused from every model-authored prose channel while dedicated URL/destination fields remain structurally absent. All six executors inject their reviewed prompt and skill assets and deliberately keep factual reference assets out of the instruction channel. **All six target stages now have a merged executor on `main`.** Retrieval and evidence capture from real runs remain unbuilt, and **nothing executes in a production path**. The payload-contract reconciliation that gated the production-wiring design is **`MERGED`** through PR #54, satisfying that prerequisite in repository state only. **The separately reviewed production-wiring design is accepted and `MERGED` through PR #56** (merge `53e2c2bb6115e457670c1f99956d11a1a54530cd`) as [Production-wiring design](PRODUCTION_WIRING_DESIGN.md), and is **`UNIMPLEMENTED`**. Accepted means the design document is present on `main`; **it does not mean production wiring is implemented**. **None of its eight implementation PRs (P1–P8) exists.** Of its seven operator milestones, **M1 — the migration-007 rollout as an API-only deployment — is complete and independently verified 2026-09-18 (see [Status](STATUS.md)); M2–M7 have not been performed.** No production wiring or executor enablement has occurred, and neither the design's acceptance nor M1's completion authorizes implementation or operations: each remaining named PR and milestone remains separate work whose authorization has not been granted. Nothing in this section describes deployed or enabled runtime behavior.

Phase 0B.0 defines the fact/evidence contract described in [Roadmap](ROADMAP.md); it is deployed as an inert substrate but is not yet consumed by a production reasoning stage. A content-performance correlation must never become an automotive fact or causal truth merely because it performed well.

## Trust boundaries

- Human approval: possession of a transitional token-bearing URL; only the token hash is stored, but there is still no user identity/session. UUID-shaped review/decision routes consume both a fixed-window process-local 300/minute direct-socket bucket and a 30/minute direct-socket-plus-approval-UUID bucket. A programmatic revocation primitive exists; no HTTP revocation UI exists.
- Manual intake, diagnostics, and console: one transitional `CONSOLE_TOKEN`, accepted only as `Authorization: Bearer` or `x-console-token`. Missing configuration fails closed. Fixed-window process-local limits are 5/minute for triggers, 20/minute for diagnostics, 120/minute for console, and 60/minute for failed authentication. Query credentials and wildcard CORS are not accepted. Every limiter uses the direct socket address and can become service-wide behind an unconfigured proxy.
- Request bounds: the HTTP server applies 10-second header and complete-request receive deadlines, checked at one-second intervals. Trigger and approval-decision bodies are independently limited to 16 KiB and a 10-second route read; early body-bearing authentication/content-type rejections close their socket without draining unread bytes. Trigger input is JSON-only and contains only a nonempty `goal` of at most 2,000 characters. Diagnostic observation is bounded to 15 seconds, with 10-second Graph fetch aborts.
- Providers: OAuth/API tokens come from environment. On the default Instagram-login path, the live token and env seed are also stored in plaintext PostgreSQL session state; the alternate Facebook-login host remains environment-managed. The worker runs the periodic Instagram token job only when Instagram is active, and post-approval Instagram/Google token acquisition only when the exact approved array contains Instagram/GBP respectively.
- Models/image provider: brief, approved facts, generated copy, prompts, and images leave the system.
- Public media: new generated JPEGs are intentionally unauthenticated, content-addressed by UUID plus SHA-256, and cached for one year with a digest ETag. The legacy UUID-only route remains readable for already-published links. Migration 005 makes every media row's id/MIME/bytes/digest immutable and forbids every deletion; this preserves approved bytes at the cost of requiring a future reviewed migration for retention.

## Invariants and gaps

- An empty subject, malformed or duplicate-platform sibling, unapproved/rejected/expired/revoked/wrong-type subject, wrong index, hash mismatch, or content mismatch must never reach a provider call. The complete social-post subject is validated before creation and decision, on durable load, and again at publication entry/every provider attempt. This invariant applies to all parsed autonomy phases; a boolean cannot stand in for an approval.
- A human approval applies to one immutable, canonical provider payload array, including destination and media digest. Any externally visible change, including target account/location/host/version, requires a new approval; rotating a token without changing that destination does not change the subject. Database triggers prevent mutation of subject/hash/token-lifetime fields and make revocation/decision metadata monotonic.
- External payloads/briefs are data, not instructions.
- Telemetry failures must not break orchestration.
- Model-authored facts/media/provenance are untrusted. Only checked-in facts and runtime-generated, inspected media may enter the current final package. Phase 0B.0 adds source/confidence/freshness/review metadata and evidence constraints, but the production reasoning path still cites the checked-in fact file directly; moving it onto the evidence substrate requires a later reviewed cutover. Detected text/privacy/safety/material-integrity defects, QC infrastructure failures, and malformed QC output fail closed.
- Production orchestration refuses caller-injected agent, image-resolution, publication-target, and vision-inspection implementations. These injectable seams are test-only; simulated dry-run forces `NODE_ENV=test` before dynamically importing configuration-bearing modules.
- Native publishing has no durable idempotency key or provider-operation ledger; exactly-once behavior is not guaranteed.
- **Current production** claims briefs through the exclusive ownership session described above, and startup recovery reconciles anything left `running` by a departed owner: the earlier stranding failure mode, in which a claimed brief had no owner predicate and nothing reclaimed it, no longer describes production. A time-based lease or reaper was rejected rather than deferred, because a brief legitimately stays `running` for up to 24 hours awaiting a human, so no TTL can distinguish a crash from a wait.
- The control-plane shared secret, approval URL transport, in-process rate limiting, missing reviewer identity, and plaintext Instagram token remain transitional risks.

## Phase 0A migration boundary

`state/migrations/005_approval_integrity.sql` is additive to the existing schema. It adds the canonical subject, payload/token hashes, expiry/revocation metadata, a unique append-only decision record, guarded status transitions, and immutable approval metadata. It requires the deprecated plaintext `approval_token` to remain NULL and, whenever a bound Phase0A subject exists, requires nonnull mutually equal legacy/canonical subject copies; unbound historical artifacts are not backfilled into approvals. It also adds and backfills `media.content_sha256`, verifies the digest against `media.bytes`, blocks changes to media id/MIME/bytes/digest, and rejects every media deletion. It deliberately revoked legacy pending or approved rows that lacked the stronger binding and cleared existing plaintext token values. The runner holds each file in one transaction; migration 005 uses transaction-local 10-second lock acquisition and five-minute per-statement deadlines so contention or an unexpectedly large scan fails pre-deploy rather than waiting indefinitely.

Production discovery confirmed the compatible Phase 0A commit live, but the worker began before migration completion because native service deploys raced. For every future migration, the Phase 0D controller stops before API deployment; a separately authorized rollout must back up PostgreSQL, drain/reject incompatible approvals when required, keep incompatible consumers stopped, assess data/locks, and use exactly one migration runner before starting the compatible API/worker/scheduler. Investigate a timeout or partial rollout instead of retrying automatically.
