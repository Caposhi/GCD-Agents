# dependencies
node_modules/

# build output
dist/

# env / secrets — real values live in Render dashboard, never committed
.env
.env.local
*.local

# logs / OS
*.log
.DS_Store

# AgentShield reports
agentshield-report.json

# local Content Intelligence CLI (scripts/local/content-run.mjs) —
# operator-supplied real automotive facts and per-run output; never committed
config/automotive-facts.local.json
local-output/
